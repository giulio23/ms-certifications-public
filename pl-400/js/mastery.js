/*
 * PL-400 mastery guides — grounded in the verified PL-400 COURSE-MAP (live Microsoft Learn docs).
 * Original, publishable content. Rendered by learn.html / learn-app.js.
 */
const MASTERY_DOMAINS = [
  {
    "n": 1,
    "title": "Create a technical design",
    "weight": "10–15%",
    "color": "blue"
  },
  {
    "n": 2,
    "title": "Build Power Platform solutions",
    "weight": "10–15%",
    "color": "emerald"
  },
  {
    "n": 3,
    "title": "Implement Power Apps improvements",
    "weight": "10–15%",
    "color": "amber"
  },
  {
    "n": 4,
    "title": "Extend the user experience",
    "weight": "10–15%",
    "color": "violet"
  },
  {
    "n": 5,
    "title": "Extend the platform",
    "weight": "30–35%",
    "color": "rose"
  },
  {
    "n": 6,
    "title": "Develop integrations",
    "weight": "10–15%",
    "color": "cyan"
  }
];

const MASTERY = [
  { id: "1.1", domain: 1, title: "Design the technical architecture", ready: true,
    intro: "This sub-skill is the architect's decision layer of PL-400: given a requirement, you choose which solution components to build, how callers authenticate, whether to reuse out-of-the-box capability or write code, where each piece of business logic belongs, which Dataverse table type fits the data, and how DLP and the Dataverse security model shape what your code can touch.",
    html: "<h2>Notes</h2><h3>Solution components &amp; out-of-the-box vs custom</h3><ul><li>Start from the requirement and reach for the <b>lowest-effort component that meets it</b>: configure with out-of-the-box features first, extend with low-code next, and only write pro-dev code when configuration cannot express the logic. This keeps solutions upgrade-safe and cheaper to support.</li><li>Out-of-the-box (declarative) building blocks: <b>tables, columns, relationships, business rules, model-driven forms/views, and cloud flows</b>. Custom (code) building blocks: <b>plug-ins, custom APIs, PCF code components, client script, custom connectors, and Azure Functions</b>.</li><li>Package everything in a <b>solution</b> with a custom <b>publisher and prefix</b> so components move together through dev &rarr; test &rarr; prod. Develop <b>unmanaged</b>; ship <b>managed</b>.</li><li>Prefer custom code only for: transactional server-side rules that must always run, complex integrations, reusable UI controls, or long-running/asynchronous processing that a flow or business rule cannot handle reliably.</li></ul><h3>Authentication &amp; authorization</h3><ul><li><b>Authentication</b> (who you are) into Dataverse is <b>Microsoft Entra ID + OAuth 2.0</b>. Interactive users sign in; unattended integrations register an <b>Entra app</b> and connect as an <b>application user</b> using a client secret/certificate (server-to-server, S2S) via <b>MSAL</b>.</li><li><b>Authorization</b> (what you can do) is enforced by the <b>Dataverse security model</b>: <b>security roles</b> grant <b>privileges</b> (Create/Read/Write/Delete/Append/AppendTo/Assign/Share) at <b>access levels</b> scoped by <b>business units</b>.</li><li>Application users must be assigned a <b>security role</b> just like humans &mdash; least-privilege applies to code identities too.</li><li>Plug-ins normally run in the <b>calling user's context</b> and are therefore constrained by that user's roles; run <b>in the context of a specific user</b> (impersonation) only when elevated access is required by design.</li></ul><h3>Where to implement business logic</h3><ul><li>Dataverse offers several tiers for logic. Choose by <b>where it must run</b> (client vs server), <b>whether it must always run</b> regardless of entry point, and <b>synchronous vs asynchronous</b> needs.</li><li><b>Business rules</b> run on the form (client) and on the server for the covered table &mdash; declarative field logic (show/hide, set value, validate) with no code.</li><li><b>Client script (JavaScript, Client API)</b> is UI-only logic in model-driven forms; it never fires for API/import paths, so never rely on it for data integrity.</li><li><b>Plug-ins</b> execute server-side in the <b>event pipeline</b> for <b>every</b> data operation (UI, API, import, flow) &mdash; the place for authoritative, transactional rules and pre/post validation.</li><li><b>Power Automate cloud flows</b> suit orchestration, approvals, notifications, and integration &mdash; asynchronous by nature and easier to maintain than code.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Logic tier</th><th>Runs where</th><th>Fires for all entry points?</th><th>Best for</th></tr></thead><tbody><tr><td><b>Business rule</b></td><td>Client + server</td><td>Yes (server side)</td><td>No-code field logic, validation, defaults</td></tr><tr><td><b>Client script (Client API)</b></td><td>Client only (form)</td><td>No (UI only)</td><td>Interactive UX, real-time form behavior</td></tr><tr><td><b>Plug-in</b></td><td>Server (event pipeline)</td><td>Yes</td><td>Authoritative, transactional, sync or async rules</td></tr><tr><td><b>Cloud flow (Power Automate)</b></td><td>Cloud (server)</td><td>No (trigger-based)</td><td>Async orchestration, approvals, integration</td></tr><tr><td><b>Real-time / async workflow</b></td><td>Server</td><td>Yes (defined events)</td><td>Legacy process automation, on-demand actions</td></tr></tbody></table></div><h3>Standard vs virtual vs elastic tables</h3><ul><li><b>Standard tables</b> store rows in Dataverse's relational store (Azure SQL). They support the full feature set: relationships, rollups, auditing, transactions, and the ownership model (user/team-owned or organization-owned). This is the default choice.</li><li><b>Virtual tables</b> surface <b>external data</b> as Dataverse rows at runtime through a <b>virtual connector / data provider</b> &mdash; the data is <b>not</b> copied into Dataverse. Good for reading external systems without duplication, but they carry limitations (limited CRUD, no auditing/rollups, no offline, keyed to an external unique identifier).</li><li><b>Elastic tables</b> are backed by <b>Azure Cosmos DB</b> for <b>very high volume, high-throughput, or schema-flexible (sparse/JSON)</b> workloads that need automatic horizontal scale. They favor point operations and partitioning (<b>partitionid</b>) over complex relational queries and do <b>not</b> support true multi-table transactions the way standard tables do.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Table type</th><th>Storage</th><th>Choose when</th><th>Key limits</th></tr></thead><tbody><tr><td><b>Standard</b></td><td>Dataverse relational (SQL)</td><td>Default; relational, transactional, full features</td><td>Not ideal for massive sparse/high-write volumes</td></tr><tr><td><b>Virtual</b></td><td>External system (no copy)</td><td>Read/expose external data without duplication</td><td>Feature-limited; needs data provider &amp; external key</td></tr><tr><td><b>Elastic</b></td><td>Azure Cosmos DB</td><td>Huge volume, high throughput, flexible/JSON schema</td><td>Partition-oriented; limited relational/transaction support</td></tr></tbody></table></div><h3>Impact of security features on the design</h3><ul><li><b>Data loss prevention (DLP) policies</b> classify connectors as <b>Business</b>, <b>Non-business</b>, or <b>Blocked</b>; connectors in different groups <b>cannot be used together</b> in the same app or flow. DLP can block your <b>custom connector</b> or an HTTP action at <b>design time and runtime</b>, so validate integration designs against tenant/environment policies early.</li><li><b>Security roles + privileges + access levels</b> gate every operation your code performs; test with a least-privilege role, not a System Administrator.</li><li><b>Business units</b> partition data ownership and set the boundary for access levels (User &lt; Business Unit &lt; Parent-Child BU &lt; Organization).</li><li><b>Teams</b> (owner teams and Entra group teams) let you assign roles and share records to groups of users; owner teams can own records directly.</li><li><b>Row (record) sharing</b> grants access to a specific row to a user or team beyond what their role gives &mdash; powerful but, at scale, a performance and manageability cost, so prefer roles/BUs/teams for broad access and use sharing for exceptions.</li></ul><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li><b>Configure before you code</b>: business rules and flows before plug-ins; plug-ins only when logic must run server-side for <b>every</b> entry point or be transactional.</li><li><b>Client script never enforces data integrity</b> &mdash; it does not run for API, import, or flow operations. Put authoritative rules in a <b>plug-in</b>.</li><li>Auth to Dataverse is <b>Entra ID / OAuth</b>; unattended code uses an <b>application user</b> (S2S) that <b>must still hold a security role</b>.</li><li>Table pick: <b>standard</b> = relational/transactional default; <b>virtual</b> = external data, no copy; <b>elastic</b> = Cosmos DB, massive/high-throughput/flexible schema.</li><li><b>DLP groups can't mix</b>: a Business connector and a Non-business connector cannot coexist in one flow/app, and DLP can block custom connectors at design and run time.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>A rule must reject any Order over a credit limit no matter whether the row is created from the model-driven app, a bulk import, or a Power Automate flow. Where do you implement it? <span class=\"check\">Decide, then check.</span></li><li>An app must show live inventory that lives in an external ERP database; the business does not want the data duplicated into Dataverse. Which table type and integration approach? <span class=\"check\">Decide, then check.</span></li><li>A nightly unattended integration service needs to write rows to Dataverse. How do you authenticate it and grant only the access it needs? <span class=\"check\">Decide, then check.</span></li><li>A flow needs to read from a Business-classified Dataverse connector and post to a custom connector that DLP has placed in the Non-business group. Why does it fail and what are your options? <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>A <b>synchronous plug-in</b> registered on the Create/Update message (Pre-Operation/Pre-Validation), because only server-side pipeline logic fires for <b>all</b> entry points and can throw to cancel the transaction. A business rule could set/validate a field but a plug-in is the authoritative transactional choice; client script would miss the import and flow paths.</li><li>A <b>virtual table</b> backed by a <b>virtual connector / data provider</b> pointing at the ERP source &mdash; rows are surfaced at query time with no copy. Accept the trade-offs (limited CRUD, no auditing/rollups, needs a stable external key).</li><li>Register an <b>Entra app</b>, add it to Dataverse as an <b>application user</b> (server-to-server auth via MSAL with a certificate/secret), and assign a <b>custom least-privilege security role</b> granting only the privileges/tables it needs &mdash; not System Administrator.</li><li>DLP forbids mixing connectors from different groups in one flow, so <b>Business (Dataverse) + Non-business (custom)</b> cannot coexist. Options: work with an admin to reclassify the custom connector into the same group, adjust the environment/tenant DLP policy, or re-architect (e.g., route through an approved connector or a plug-in/Azure Function that the policy permits).</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>Which logic tier runs for <b>every</b> data entry point (UI, API, import, flow) and can enforce transactional integrity?</li><li>Give one reason to choose an <b>elastic</b> table over a <b>standard</b> table, and one trade-off you accept.</li><li>How does an unattended integration authenticate to Dataverse, and what must it be granted before it can operate?</li><li>What happens when a flow tries to combine a <b>Business</b> connector and a <b>Non-business</b> connector under a DLP policy?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li>A <b>plug-in</b> in the Dataverse event pipeline (server-side); business rules also run server-side but a synchronous plug-in is the authoritative transactional choice.</li><li>Choose elastic for <b>very high volume/throughput or flexible (sparse/JSON) schema with automatic horizontal scale</b> (Cosmos DB); the trade-off is <b>limited relational querying/transactions</b> and a partition-oriented data model.</li><li>Via <b>Microsoft Entra ID / OAuth 2.0</b> as an <b>application user</b> (server-to-server, MSAL with secret or certificate); it must be <b>assigned a security role</b> with least-privilege privileges before it can read or write.</li><li>The policy <b>blocks it</b> &mdash; connectors from different DLP groups cannot be used together in the same flow or app, at design time and run time.</li></ol></details>" },
  { id: "1.2", domain: 1, title: "Design solution components", ready: true,
    intro: "Given a set of requirements, choose the right extensibility building block — reusable UI, code components, Dataverse logic, automation, or integration — and justify low-code versus pro-code. This is a design-time decision skill: you match each need to the component type that fits its trigger, scope, and reuse pattern.",
    html: "<h2>Notes</h2><h3>Reusable Power Apps components</h3><p>Power Apps offers three distinct reuse mechanisms; choosing between them is a common exam decision.</p><ul><li><b>Canvas components</b> are low-code, reusable building blocks authored with Power Fx. They expose <b>custom input and output properties</b> so a maker can pass data in and read values out, and they are shared across screens (in-app) or across apps via a <b>component library</b>. Use them for a repeated header, nav menu, or a branded control that can be built entirely with formulas.</li><li><b>PCF code components</b> (Power Apps component framework) are pro-developer components written in <b>TypeScript/JavaScript</b> and packaged with the Power Platform CLI. They run in <b>both model-driven and canvas apps</b>, can bind to a single field or a whole dataset (grid), and can call platform APIs the low-code layer cannot. Choose PCF when you need a rich custom visual, third-party JS library, or behavior beyond what Power Fx allows.</li><li><b>Client scripting</b> (JavaScript with the Client API) attaches business logic to <b>model-driven app form and grid events</b> (OnLoad, OnChange, OnSave). It manipulates the form at runtime but does not create a reusable visual control — use it for conditional UI, validation, and calling the Web API from the form.</li></ul><h3>Custom connectors</h3><ul><li>A <b>custom connector</b> wraps a REST API (your own service, an Azure Function, or a third-party API) so it becomes callable from <b>canvas apps, cloud flows, and Copilot Studio</b> like any standard connector.</li><li>It is described by an <b>OpenAPI (Swagger) definition</b> or a Postman collection, secured with an authentication scheme (No auth, API key, Basic, OAuth 2.0), and progresses through a lifecycle: <b>build, secure, describe, test, share, and optionally certify</b>.</li><li>Use a custom connector for <b>outbound integration</b> from the platform to an external HTTP API when no standard connector exists.</li></ul><h3>Dataverse code components</h3><ul><li><b>Power Fx functions / low-code plug-ins</b> apply logic close to the data using formulas — good for lightweight server-side calculations and automations without C#.</li><li><b>Plug-ins</b> are C# classes (<code>IPlugin</code>) registered on the Dataverse event pipeline. They run <b>synchronously or asynchronously</b> on data events and are the right choice for transactional business logic, validation that must block a save, and operations that need Pre/Post images.</li><li><b>Custom APIs</b> define a <b>reusable, named message</b> (a custom verb) in Dataverse with typed request/response parameters. They are <b>code-first</b> (defined via solution tables or the CustomAPI entity) and bound to a plug-in that implements the logic, giving you a first-class, discoverable operation callable from the Web API, plug-ins, flows, and client code. Prefer a custom API over a legacy Action when you want a code-defined, versionable message with private/protected access control.</li></ul><h3>Automations and integrations</h3><ul><li><b>Cloud flows</b> in Power Automate orchestrate work with a <b>trigger</b> (automated on an event, instant/manual, or scheduled) followed by <b>actions</b> across connectors. Use them for approvals, notifications, and cross-service orchestration that does not need the transactional guarantees of a plug-in.</li><li><b>Inbound</b> integration brings external data/events into Dataverse (custom API called by an outside system, cloud flow triggered by an external event, virtual tables). <b>Outbound</b> integration sends data out (custom connector, HTTP action, service endpoints/webhooks). Match the direction and latency need to the component.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Requirement</th><th>Best component</th><th>Why</th></tr></thead><tbody><tr><td>Branded, reusable UI built with formulas, shared across apps</td><td>Canvas component in a component library</td><td>Low-code, custom input/output properties, no code build</td></tr><tr><td>Rich custom control / dataset grid in a model-driven form</td><td>PCF code component</td><td>TypeScript, runs model-driven &amp; canvas, binds field or dataset</td></tr><tr><td>Conditional form behavior &amp; validation on a model-driven form</td><td>Client scripting (Client API)</td><td>Handles form/grid events; not a reusable visual</td></tr><tr><td>Call an external REST API from a flow or canvas app</td><td>Custom connector</td><td>OpenAPI wrapper, auth, reusable across makers</td></tr><tr><td>Reusable named server operation with typed parameters</td><td>Custom API + plug-in</td><td>Code-first message, callable from Web API/flows/code</td></tr><tr><td>Blocking transactional business logic on a data event</td><td>Synchronous plug-in</td><td>Runs in the transaction; can cancel the operation</td></tr><tr><td>Approval / notification orchestration across services</td><td>Cloud flow</td><td>Trigger + multi-connector actions, low-code</td></tr></tbody></table></div><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li><b>Canvas component vs PCF:</b> canvas components are low-code Power Fx and canvas-only; PCF code components are TypeScript and run in <b>both</b> model-driven and canvas apps.</li><li><b>Custom API vs cloud flow:</b> a custom API is a <b>synchronous, code-first Dataverse message</b> (transactional, callable programmatically); a cloud flow is <b>asynchronous low-code orchestration</b>. Don't use a flow where a plug-in/custom API is required to block or guarantee a transaction.</li><li><b>Custom connector = outbound wrapper</b> around a REST API described by an <b>OpenAPI definition</b>; it makes an external API reusable across canvas apps and flows.</li><li><b>Client scripting</b> targets model-driven form/grid events and is <b>not</b> a packaged reusable control — reach for PCF when you need a distributable component.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>A sales team wants an identical, branded contact-card control with a photo, formatted name, and a call button reused on five screens of one canvas app and in two other apps. It can be built entirely with formulas. Which reuse mechanism do you pick, and how do you share it across apps? <span class=\"check\">Decide, then check.</span></li><li>A model-driven main form must show a color-coded gauge visual bound to a numeric field, using a JavaScript charting library. What component type fits, and why is client scripting alone insufficient? <span class=\"check\">Decide, then check.</span></li><li>An external ERP must, on demand, ask Dataverse to run a multi-step \"recalculate credit limit\" operation with typed inputs/outputs, synchronously, callable from both the ERP and internal plug-ins. What do you design? <span class=\"check\">Decide, then check.</span></li><li>Managers need an email approval whenever an opportunity's estimated value exceeds a threshold, routing to a manager and posting the result to Teams. No transactional blocking is required. What component, trigger, and integration style do you choose? <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>A <b>canvas component</b> with custom input properties (contact record) placed in a <b>component library</b> so it can be imported and version-updated across the other apps. Low-code and formula-only, so no PCF build is needed.</li><li>A <b>PCF code component</b> bound to the field. Client scripting can react to events but cannot package a reusable, distributable custom visual or host a third-party rendering library as a bound control; PCF provides the manifest, TypeScript implementation, and runs in the model-driven form.</li><li>A <b>custom API</b> defining the named message with request/response parameters, bound to a <b>synchronous plug-in</b> that implements the logic. It is code-first, transactional, and callable from the Web API (by the ERP) and from other server-side code — an inbound integration point.</li><li>An <b>automated cloud flow</b> triggered by \"When a row is added or modified\" with a trigger/filter condition on estimated value, using the Approvals action and a Teams action. Low-code orchestration is appropriate because no in-transaction blocking is needed.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>True or false: a canvas component can be used inside a model-driven app form the same way a PCF code component can.</li><li>Which artifact describes the REST API that a custom connector wraps, and name two authentication options it supports.</li><li>You need a reusable, named Dataverse operation with typed parameters that runs synchronously and can be called from external code. Which two components do you combine?</li><li>Give one inbound and one outbound integration example a solution designer might choose, naming the component for each.</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li><b>False.</b> Canvas components are canvas-app only (low-code Power Fx). <b>PCF code components</b> are the ones that run in both model-driven and canvas apps.</li><li>An <b>OpenAPI (Swagger) definition</b> (or Postman collection). Auth options include <b>No authentication, API key, Basic, and OAuth 2.0</b>.</li><li>A <b>custom API</b> (the message definition) bound to a <b>plug-in</b> (the implementation), registered synchronously.</li><li>Inbound: an external system calling a <b>custom API</b> (or a flow triggered by an external event) to write into Dataverse. Outbound: a <b>custom connector</b> (or HTTP action / webhook) sending data from Dataverse to an external REST service.</li></ol></details>" },
  { id: "2.1", domain: 2, title: "Configure and troubleshoot Power Platform environments", ready: true,
    intro: "As a developer you rarely own the tenant, but you must configure development environments, register application users, and grant code the least privilege it needs to run. This sub-skill covers environment types, security roles for code components, and diagnosing operational security failures.",
    html: "<h2>Notes</h2><h3>Environment types and development environments</h3><ul><li>An <b>environment</b> is a container for apps, flows, connections, and (optionally) a <b>Dataverse</b> database. Types include <b>Production</b>, <b>Sandbox</b>, <b>Developer</b>, <b>Trial</b>, and <b>Default</b>.</li><li>A <b>Developer</b> environment is a personal, single-user environment (created from the Power Apps Developer Plan or from the admin center). It always has Dataverse, is free, and is meant for building and testing — never for shared production workloads.</li><li>Use <b>Sandbox</b> environments for team development, testing, and as pipeline targets: they can be <b>reset</b>, <b>copied</b>, and <b>restored</b>, which Production environments restrict.</li><li>Creating a Dataverse-backed environment requires a <b>security group</b> decision, a <b>currency</b> and <b>language</b>, and enough capacity. Restrict membership with a Microsoft Entra security group so only intended developers get access.</li><li>Only tenant/environment admins can create environments; the maker of a Developer environment is automatically its <b>System Administrator</b>.</li></ul><h3>Application users and server-to-server (S2S) auth for code</h3><ul><li>Server-side code (plug-ins calling out, Azure Functions, custom connectors, integration services) authenticates to Dataverse as an <b>application user</b>, not as a licensed human user.</li><li>Register an app in <b>Microsoft Entra ID</b>, then create a matching <b>application user</b> in the environment bound to that app's <b>Application (client) ID</b>. This is <b>single-tenant S2S authentication</b>.</li><li>Application users are <b>not licensed</b> and do not consume a seat, but they have <b>no privileges</b> until you assign one or more <b>security roles</b>.</li><li>Prefer S2S application users over embedding a real user's credentials — it avoids license consumption, survives the person leaving, and can be scoped tightly.</li></ul><h3>Security roles for code components and least privilege</h3><ul><li>Dataverse authorization is <b>role-based</b>. A <b>security role</b> is a set of <b>privileges</b> (Create, Read, Write, Delete, Append, Append To, Assign, Share) each granted at an <b>access level</b>: None, User (Basic), Business Unit (Local), Parent: Child (Deep), or Organization (Global).</li><li>Grant code the <b>minimum</b> table privileges and access levels it actually needs. If a plug-in only reads accounts and writes tasks, its application user's role should not include Delete on accounts or Organization-level write everywhere.</li><li>Users and application users are members of a <b>business unit</b> and can belong to <b>teams</b>; roles assigned to a team apply to its members. Use <b>owner teams</b> or <b>Microsoft Entra group teams</b> to manage role assignment at scale.</li><li>Build a dedicated <b>custom security role</b> for each integration/application user rather than reusing System Administrator — copy a role and trim it. Ship the custom role inside the solution so it deploys with the code.</li><li>Plug-ins registered to run in a <b>specific user context</b> execute with that user's privileges; plug-ins running as the <b>calling user</b> inherit the caller's role — either way the effective privilege set is the union of the running identity's roles.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Access level (Read on a table)</th><th>Effective scope</th><th>Least-privilege fit</th></tr></thead><tbody><tr><td>User (Basic)</td><td>Only records the identity owns</td><td>Tightest; personal/self-service code</td></tr><tr><td>Business Unit (Local)</td><td>Records owned in the same business unit</td><td>Departmental integration</td></tr><tr><td>Parent: Child (Deep)</td><td>Own BU plus child BUs</td><td>Regional/hierarchy integration</td></tr><tr><td>Organization (Global)</td><td>Every record in the environment</td><td>Only when the code genuinely needs tenant-wide reach</td></tr></tbody></table></div><h3>Troubleshooting operational security issues</h3><ul><li>A <b>Principal user is missing prvReadX</b> / privilege error means the running identity's roles lack a specific privilege or access level — add that privilege to the identity's <b>custom role</b>, do not grant System Administrator.</li><li>If an <b>application user</b> gets 401/403 or \"does not exist\", verify the Entra app registration's client ID matches the application user record, the secret/certificate is valid, and at least one security role is assigned.</li><li>Missing <b>Append</b> or <b>Append To</b> privileges break relationship/lookup writes even when Create/Write succeed — check both sides of the relationship.</li><li>Access unexpectedly wide? Someone may have granted an <b>Organization</b>-level role or added the identity to a privileged team; audit role and team membership, not just the direct role.</li><li>Use the environment's <b>security role</b> and <b>audit</b> views in the admin center to trace what an identity can actually do before changing code.</li></ul><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li>Application users are unlicensed but have <b>zero access until a security role is assigned</b> — a freshly registered app user always fails Dataverse calls until then.</li><li>Least privilege = a <b>custom security role scoped to the exact tables, privileges, and access levels</b> the code needs; never reuse System Administrator for integrations.</li><li>S2S code identity = <b>Entra app registration</b> whose <b>Application (client) ID</b> is bound to a Dataverse <b>application user</b>.</li><li>Use <b>Developer/Sandbox</b> environments for building; Sandbox can be reset/copied/restored, Production cannot.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>A nightly Azure Function must read Contacts and create Cases in Dataverse but nothing else. How do you give it access with least privilege? <span class=\"check\">Decide, then check.</span></li><li>A newly registered application user returns 403 on every Web API call even though the Entra secret is valid. What is the most likely cause and fix? <span class=\"check\">Decide, then check.</span></li><li>Your team needs an isolated place to build and iterate on a plug-in without touching the shared test environment or consuming production capacity. Which environment do you use? <span class=\"check\">Decide, then check.</span></li><li>A plug-in that writes a lookup from Order to Account throws a privilege error, yet the role clearly has Create and Write on Order. What privilege is missing? <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>Register an Entra app, create a bound <b>application user</b>, and assign it a <b>custom security role</b> granting only <b>Read on Contact</b> and <b>Create/Read on Case</b> at the narrowest access level that works (Business Unit or Organization only if required) — no Delete, no other tables.</li><li>The application user has <b>no security role assigned</b>. Registration and a valid secret authenticate it, but Dataverse authorization needs at least one role; assign a scoped custom role.</li><li>A <b>Developer environment</b> (Power Apps Developer Plan): free, single-user, Dataverse-backed, isolated — ideal for personal build/test without shared or production impact.</li><li><b>Append To</b> on Account (and <b>Append</b> on Order) — setting a lookup requires append privileges on both related tables, independent of Create/Write.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>True or false: an application user consumes a Power Platform license seat.</li><li>Which access level lets code read every record of a table across the whole environment?</li><li>What is the recommended role strategy for an integration's application user instead of using System Administrator?</li><li>Which environment type can be reset, copied, and restored to support team development and pipeline targets?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li><b>False.</b> Application users are unlicensed; they authenticate via an Entra app registration and gain access only through assigned security roles.</li><li><b>Organization (Global)</b> access level — grant it only when the code genuinely needs tenant-wide reach.</li><li>Create a <b>dedicated custom security role</b> scoped to just the tables, privileges, and access levels the code needs, and ship it in the solution — never assign System Administrator to integration code.</li><li>A <b>Sandbox</b> environment (Production restricts reset/copy/restore).</li></ol></details>" },
  { id: "2.2", domain: 2, title: "Implement application lifecycle management (ALM)", ready: true,
    intro: "ALM is how you move a Power Platform build from a Dev environment to Test and Production reliably and repeatably. For PL-400, know the mechanics cold: managed vs unmanaged solutions, how solution layers merge, tracking and packaging dependencies, environment variables for per-environment config, and the two shipping paths - in-product Power Platform Pipelines and code-first CI/CD with the Power Platform Build Tools for Azure DevOps.",
    html: "<h2>Notes</h2>\n<h3>Solutions, managed vs unmanaged</h3>\n<ul>\n<li>A <b>solution</b> is the ALM unit of transport - it packages components (tables, columns, apps, flows, plug-in assemblies, web resources, environment variables) so they move together between environments. Every environment has a <b>Default</b> solution; you build in a custom <b>unmanaged</b> solution.</li>\n<li><b>Unmanaged</b> = the source/development state. Components are editable, deletions apply, and the solution is a container - deleting an unmanaged solution leaves its components behind. You develop only in unmanaged solutions.</li>\n<li><b>Managed</b> = the deployment/distribution state. Import into Test and Production as managed. Components are locked to configured customization, the solution is a real boundary, and <b>deleting a managed solution removes its components and their data</b> (clean uninstall).</li>\n<li>You author unmanaged, then <b>export as managed</b> for downstream environments. <b>Never import unmanaged into production.</b> A given environment should be either a maker/dev environment (unmanaged) or a target (managed) - not both for the same solution.</li>\n<li><b>Solution Packager (SolutionPackager / <code>pac solution</code>)</b> unpacks a solution .zip into source files for source control and repacks it - the basis of source-controlled ALM.</li>\n</ul>\n<h3>Solution layers &amp; merge behavior</h3>\n<ul>\n<li>Every component is the sum of its <b>layers</b>. <b>Managed layers</b> sit at the bottom in import order (the base managed solution, then each managed solution on top); the <b>unmanaged layer</b> sits on top of all managed layers.</li>\n<li><b>Merge across managed layers:</b> most properties are <b>top-wins</b> (the last-imported managed solution's value wins), but a few UI-composed components - <b>forms, site maps, ribbons/command bars, and app modules - are MERGED</b> across layers rather than replaced, so a change lower in the stack can still show.</li>\n<li>An <b>unmanaged (active) layer</b> always overrides everything beneath it. An unmanaged customization made directly in a target environment shadows the managed value and can block later managed updates from appearing.</li>\n<li>Use <b>Solution Layers</b> (in the maker portal, per component) to see the ordered stack and diagnose why a value is what it is. <b>Remove active customization</b> deletes the top unmanaged layer so the managed value below takes effect again.</li>\n</ul>\n<h3>Solution dependencies</h3>\n<ul>\n<li>A <b>dependency</b> is a required relationship between components - e.g. a form depends on the column it shows, a plug-in step depends on the message/table it fires on, one solution's component references another solution's component.</li>\n<li>Dependencies are enforced at import and delete time: you <b>cannot import</b> a solution whose <b>required components are missing</b> in the target, and you <b>cannot delete/uninstall</b> a managed solution while another solution still <b>depends</b> on its components. Dataverse tracks these automatically.</li>\n<li>Use <b>Show Dependencies</b> on a component (and the <b>Solutions &gt; Dependencies</b> views) before deleting or exporting. Package prerequisites first, or use a <b>base + extension</b> solution layering so shared components ship once.</li>\n<li><b>Managed properties</b> control whether downstream makers may customize a component (e.g. <code>IsCustomizable</code>); set them before exporting managed to protect what should not be changed.</li>\n</ul>\n<h3>Environment variables</h3>\n<ul>\n<li><b>Environment variables</b> store configuration that differs per environment - connection endpoints, API URLs, IDs, flags - so the <b>same solution</b> carries different values in Dev, Test, and Prod without editing components.</li>\n<li>Two parts: the <b>definition</b> (schema name, data type - Text, Number, Boolean, JSON, Data source, or <b>Secret</b>) which travels in the solution, and the <b>value</b>. A <b>default value</b> ships with the solution; a <b>current (environment) value</b> is set per target and is <b>not</b> exported, so it does not overwrite the target's own value on import.</li>\n<li>Reference them from <b>cloud flows, canvas apps (<code>Environment.Variable</code> / dataflows), plug-ins, and custom connectors</b> instead of hard-coding. This is the standard way to parameterize connection references and config across the ALM pipeline.</li>\n<li><b>Secret</b> environment variables integrate with <b>Azure Key Vault</b> - the variable references a Key Vault secret rather than storing the value in Dataverse.</li>\n</ul>\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Aspect</th><th>Unmanaged solution</th><th>Managed solution</th></tr></thead>\n<tbody>\n<tr><td>Purpose</td><td>Development / source</td><td>Deployment / distribution</td></tr>\n<tr><td>Editable in place</td><td>Yes</td><td>No (only via managed properties / new layer)</td></tr>\n<tr><td>Where used</td><td>Dev / maker environments</td><td>Test, Production, distribution</td></tr>\n<tr><td>Layer position</td><td>Top (active) layer</td><td>Managed layers below the active layer</td></tr>\n<tr><td>Deleting the solution</td><td>Container removed; components remain</td><td>Components &amp; their data removed (uninstall)</td></tr>\n<tr><td>Import to production?</td><td>No - never</td><td>Yes</td></tr>\n</tbody>\n</table></div>\n<h3>Shipping: Pipelines vs Build Tools (CI/CD)</h3>\n<ul>\n<li><b>Power Platform Pipelines</b> are the <b>in-product</b>, low-code deployment path. Configured in a dedicated <b>host environment</b> with the <b>Power Platform Pipelines</b> app; makers run deployments from inside the Dev environment. Pipelines <b>export the source solution as managed</b> and import it into the linked Test/Prod target environments, with optional <b>pre-deployment approvals</b> and stage gates - no Azure DevOps or scripting required.</li>\n<li><b>Power Platform Build Tools</b> are a set of <b>Azure DevOps</b> (and GitHub Actions equivalents) tasks for full <b>CI/CD</b>: <code>Export Solution</code>, <code>Unpack/Pack Solution</code>, <code>Import Solution</code>, <code>Publish Customizations</code>, <code>Set Solution Version</code>, <code>Deploy Package</code>, plus <b>quality gates</b> like the <b>Solution Checker</b>. They authenticate to environments through a <b>service connection</b> - a service principal (app registration) or (legacy) username/password.</li>\n<li>Typical Build Tools flow: <b>build pipeline</b> exports the unmanaged solution and unpacks it into <b>source control</b>; <b>release pipeline</b> packs, sets version, and imports the <b>managed</b> solution to Test then Prod. Set <b>environment variable</b> current values and <b>connection references</b> per stage via a <b>deployment settings file</b> so imports are unattended.</li>\n<li>Choose <b>Pipelines</b> for maker-driven, governed deployments with minimal infrastructure; choose <b>Build Tools</b> when you need source control, automated builds/tests, gated multi-stage releases, or integration with the rest of an Azure DevOps/GitHub codebase.</li>\n</ul>\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li><b>Develop unmanaged, deploy managed.</b> Never import an unmanaged solution into Test/Prod; export from Dev as managed for downstream environments.</li>\n<li>Layer merge is mostly <b>top-wins</b> (last managed import), but <b>forms, site maps, command bars/ribbons, and app modules are merged</b>; an <b>unmanaged active layer</b> always overrides managed values.</li>\n<li>Deleting a <b>managed</b> solution <b>removes its components and data</b>; deleting an unmanaged one leaves components behind.</li>\n<li>Environment variable <b>current values are not exported</b> - only the definition and default value travel; set the current value (or Key Vault secret reference) per target.</li>\n<li><b>Pipelines</b> = in-product, low-code, host environment, auto export-as-managed. <b>Build Tools</b> = Azure DevOps/GitHub CI/CD tasks authenticating via a <b>service connection (service principal)</b>.</li>\n<li>Solution <b>dependencies</b> block importing when prerequisites are missing and block uninstalling a solution others depend on - check <b>Show Dependencies</b> first.</li>\n</ul></div>\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>A team has been editing customizations directly in the Production environment. A new managed update is imported but the fixes don't appear. What is going on and how do you resolve it? <span class=\"check\">Decide, then check.</span></li>\n<li>A flow in the solution must call a different HTTP endpoint in Dev, Test, and Prod, and one action needs an API key that security requires be stored in Azure Key Vault. How do you design this so one solution deploys everywhere? <span class=\"check\">Decide, then check.</span></li>\n<li>The organization wants governed Dev-to-Test-to-Prod deployments with approvals, run by makers, and has no appetite to build and maintain Azure DevOps YAML. What deployment approach do you recommend and how is it set up? <span class=\"check\">Decide, then check.</span></li>\n<li>An automated release must export the solution from Dev, store it in Git, run Solution Checker, and import managed to Test then Prod unattended, authenticating without a user account. What tooling and auth do you use? <span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>The direct edits created an <b>unmanaged (active) layer</b> on top of the managed layers, and the active layer <b>overrides</b> the incoming managed value, so the update is hidden beneath it. Inspect <b>Solution Layers</b> for the affected components and <b>Remove active customization</b> (delete the unmanaged layer) so the managed value takes effect; going forward, stop customizing Production directly and make changes in Dev.</li>\n<li>Add <b>environment variables</b> to the solution for the endpoint URL (Text) and reference them in the flow, so each environment sets its own <b>current value</b> (which is not exported). For the API key use a <b>Secret</b> environment variable backed by <b>Azure Key Vault</b>, referencing the secret rather than storing it in Dataverse. One managed solution then deploys to all three environments with per-environment config.</li>\n<li>Use <b>Power Platform Pipelines</b>: install the <b>Pipelines</b> app in a dedicated <b>host environment</b>, define the pipeline with the Dev source and linked Test/Prod target environments, and add <b>pre-deployment approvals</b>/stage gates. Makers run the deployment from inside Dev; Pipelines exports as <b>managed</b> and imports to the targets - all in-product, no Azure DevOps required.</li>\n<li>Use the <b>Power Platform Build Tools</b> in <b>Azure DevOps</b> (or GitHub Actions): a build pipeline runs <code>Export Solution</code> + <code>Unpack</code> into Git and <b>Solution Checker</b> as a quality gate; a release pipeline runs <code>Pack</code>/<code>Set Solution Version</code>/<code>Import Solution</code> (managed) to Test then Prod, using a <b>deployment settings file</b> for connection references and environment-variable values. Authenticate with a <b>service connection using a service principal (app registration)</b>, not a user account.</li>\n</ol></details>\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Which solution type do you import into Production, and what happens to components if that solution is later deleted?</li>\n<li>Which component types are <b>merged</b> across solution layers rather than following top-wins, and what always overrides a managed value?</li>\n<li>When a solution is exported, is an environment variable's current (environment) value exported? What travels with the solution?</li>\n<li>What is the key difference between Power Platform Pipelines and the Power Platform Build Tools, and how does each authenticate to target environments?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li>Import a <b>managed</b> solution into Production. Deleting a managed solution <b>uninstalls it - removing its components and their data</b> (deleting an unmanaged solution would only remove the container and leave components behind).</li>\n<li><b>Forms, site maps, command bars/ribbons, and app modules</b> are merged across layers; everything else is largely <b>top-wins</b> (last managed import). An <b>unmanaged active layer</b> always overrides the managed value beneath it.</li>\n<li>No - the <b>current value is not exported</b>. Only the <b>definition</b> (schema, type) and the <b>default value</b> travel with the solution; you set the current value (or Key Vault secret reference) per target environment.</li>\n<li><b>Pipelines</b> are in-product and low-code, run by makers from a host environment and auto-export as managed - no Azure DevOps. <b>Build Tools</b> are Azure DevOps/GitHub CI/CD tasks for source-controlled, gated releases and authenticate through a <b>service connection (service principal / app registration)</b>.</li>\n</ol></details>" },
  { id: "3.1", domain: 3, title: "Implement advanced canvas app features", ready: true,
    intro: "Advanced canvas apps are built on three pillars the PL-400 tests directly: composing complex Power Fx (behavior vs. property formulas, collections, delegation-aware functions), packaging reusable UI in component libraries, and invoking Power Automate cloud flows from the app. Expect precise 'which function / which context / how do you call the flow' questions.",
    html: "<h2>Notes</h2>\n\n<h3>Power Fx: the formula language</h3>\n<ul>\n<li><b>Power Fx</b> is a strongly-typed, declarative, functional language. Most formulas are <b>declarative</b> — a property is an expression that <b>recalculates automatically</b> (like a spreadsheet) when its inputs change; you never write the recalculation loop.</li>\n<li>Two formula kinds: <b>property formulas</b> (compute a value, e.g. <code>Fill</code>, <code>Text</code>, <code>Items</code>) must be side-effect free; <b>behavior formulas</b> run on events (<code>OnSelect</code>, <code>OnChange</code>, <code>OnStart</code>) and <i>may</i> have side effects and chain steps with <b>;</b> (or <b>;;</b> in some locales).</li>\n<li>Side-effecting functions are only allowed in behavior contexts: <b>Set</b> (global variable), <b>UpdateContext</b> (screen/context variable), <b>Navigate</b>, <b>Collect</b>/<b>ClearCollect</b>/<b>Patch</b>/<b>Remove</b>, <b>Notify</b>, <b>Reset</b>, <b>Set focus</b>.</li>\n<li><b>State</b> options: <i>global variables</i> via <code>Set</code>, <i>context variables</i> via <code>UpdateContext</code>/<code>Navigate</code> (screen-scoped), and <i>collections</i> (in-memory tables) via <code>Collect</code>/<code>ClearCollect</code>. Prefer named formulas and variables over deeply nested duplicated logic.</li>\n</ul>\n\n<h3>Complex formulas and functions</h3>\n<ul>\n<li><b>Tables and records</b> are first-class values. Table-shaping functions: <b>Filter</b>, <b>Search</b>, <b>Sort</b>/<b>SortByColumns</b>, <b>LookUp</b>, <b>FirstN</b>/<b>LastN</b>, <b>AddColumns</b>/<b>DropColumns</b>/<b>RenameColumns</b>/<b>ShowColumns</b>, <b>GroupBy</b>/<b>Ungroup</b>, and the <b>ForAll</b> iterator.</li>\n<li><b>ForAll</b> evaluates a formula for every row of a table and returns a table of results; combine with <b>Patch</b> for bulk writes. It does <b>not</b> guarantee execution order and is <b>not</b> a general loop.</li>\n<li><b>Patch</b> creates or updates records (and can update multiple rows with a table argument); <b>Patch(DefaultRecord, Changes)</b> is the create/update pattern behind forms.</li>\n<li>Error handling: <b>IfError</b>, <b>IsError</b>, <b>Errors()</b>, and the <b>App.OnError</b> handler. Use <b>With</b> to name intermediate values and keep a big formula readable; use the <b>As</b> operator to name records in <code>ForAll</code>/<code>Filter</code> and avoid ambiguity.</li>\n<li><b>Delegation</b>: some functions/operators are <b>delegable</b> (pushed to the data source — Dataverse, SQL). Non-delegable logic runs locally on only the first <b>500</b> rows (raisable to <b>2000</b>). Non-delegable warnings on large sources are a top exam and troubleshooting theme.</li>\n</ul>\n\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Function</th><th>Purpose</th><th>Context / note</th></tr></thead>\n<tbody>\n<tr><td><b>Set</b></td><td>Set a global variable</td><td>Behavior only; app-wide scope</td></tr>\n<tr><td><b>UpdateContext</b></td><td>Set a context (screen) variable</td><td>Behavior only; single screen</td></tr>\n<tr><td><b>ClearCollect</b></td><td>Replace a collection's contents</td><td>Behavior; in-memory table, good for caching</td></tr>\n<tr><td><b>Filter / LookUp</b></td><td>Query rows / one row</td><td>Delegable to the source when possible</td></tr>\n<tr><td><b>ForAll</b></td><td>Evaluate per row, return a table</td><td>No guaranteed order; not a loop</td></tr>\n<tr><td><b>Patch</b></td><td>Create/update record(s)</td><td>Behavior; core write function</td></tr>\n<tr><td><b>IfError</b></td><td>Handle errors inline</td><td>Property or behavior</td></tr>\n</tbody>\n</table></div>\n\n<h3>Reusable component libraries</h3>\n<ul>\n<li>A <b>canvas component</b> is a reusable building block (custom header, nav menu, styled button) with <b>custom input and output properties</b>. Input properties feed data in; output properties expose values (and events) back to the host app.</li>\n<li>A <b>component library</b> is a container that stores components for reuse and <b>sharing across many apps</b> in an environment. It is the recommended way to build and distribute reusable components — the authoritative source.</li>\n<li><b>Update flow</b>: edit the component in the library and publish. Apps that consume it are notified that <b>updates are available</b>; the maker chooses to <b>Update</b> to pull the new version. Imported component copies are read-only in the consuming app unless you allow customization.</li>\n<li>Component libraries are <b>solution-aware</b>, so they move across environments through ALM. Custom properties support <b>data type</b> and <b>property type</b> (Input vs Output; and behavior/function properties that can raise events like <code>OnSelect</code>).</li>\n<li>Contrast with <b>PCF code components</b> (pro-dev, TypeScript) — component libraries are the <b>low-code</b>, Power Fx reuse mechanism.</li>\n</ul>\n\n<h3>Calling cloud flows from a canvas app</h3>\n<ul>\n<li>Add Power Automate from the app: the flow must use the <b>Power Apps (V2)</b> trigger (define typed <b>input parameters</b>; return values via <b>Respond to a Power App or flow</b>). Modern connectors surface flows via the <b>Power Automate</b> pane.</li>\n<li>Invoke with <b>FlowName.Run(arg1, arg2, ...)</b> in a <b>behavior</b> formula (e.g. <code>OnSelect</code>). Arguments map, in order, to the flow's defined inputs.</li>\n<li>If the flow uses <b>Respond to a Power App or flow</b>, <code>.Run()</code> returns a <b>record</b> of the output values, which you can capture: <code>Set(result, MyFlow.Run(TextInput.Text)); Notify(result.message)</code>.</li>\n<li>Because <code>.Run()</code> has side effects (calls a service), it must live in a behavior property, not a data/property formula. Wrap it in <b>IfError</b> to handle a failed flow call gracefully.</li>\n<li>Removing a flow from an app: use the app's data/Power Automate pane. Flows are per-app connections and travel with the app in a solution for ALM.</li>\n</ul>\n\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li>Side-effecting functions (<b>Set</b>, <b>UpdateContext</b>, <b>Collect</b>, <b>Patch</b>, <b>Navigate</b>, <b>Notify</b>, <b>.Run()</b>) are legal <b>only in behavior formulas</b> (OnSelect/OnChange/OnStart), never in property formulas.</li>\n<li>Non-delegable functions process only the first <b>500</b> rows (up to <b>2000</b> configurable) locally — design queries to <b>delegate</b> (Filter/LookUp/Sort on delegable columns).</li>\n<li><b>Component libraries</b> are the recommended way to share and centrally update reusable components; consuming apps get an <b>Update available</b> prompt.</li>\n<li>Call a flow with <b>FlowName.Run(...)</b>; the flow needs the <b>Power Apps (V2)</b> trigger, and returns data via <b>Respond to a Power App or flow</b>.</li>\n</ul></div>\n\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>A gallery bound to a 50,000-row Dataverse table shows a blue 'delegation' warning and only ever finds matches near the top of the list. What is wrong and how do you fix it?<span class=\"check\">Decide, then check.</span></li>\n<li>Five apps in your environment all need the same branded header with a title input and a 'menu clicked' event. You want to fix a bug once and roll it out everywhere. What do you build?<span class=\"check\">Decide, then check.</span></li>\n<li>A submit button must send the form data to an approval process implemented in Power Automate and then show the returned approval ID to the user. How do you wire the button?<span class=\"check\">Decide, then check.</span></li>\n<li>You need to write the OnSelect of a button to save several values app-wide, refresh a cached list, and move to the next screen. Which functions, and why can't this go in a Text property?<span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>The <b>Filter/Search/Sort</b> uses a <b>non-delegable</b> function or column, so Power Apps evaluates only the first 500 (up to 2000) downloaded rows. Rewrite the query to use <b>delegable</b> functions and operators (e.g. <code>Filter</code>/<code>LookUp</code> with <code>StartsWith</code> on a delegable column) so it runs on the Dataverse server across all rows.</li>\n<li>Build a <b>canvas component</b> with an <b>input property</b> for the title and an <b>output/behavior property</b> for the menu event, store it in a <b>component library</b>, and import it into the five apps. Fix the bug in the library, publish, and each app pulls the <b>Update available</b> version.</li>\n<li>Use a flow with the <b>Power Apps (V2)</b> trigger that ends with <b>Respond to a Power App or flow</b> returning the approval ID. In the button's <code>OnSelect</code>: <code>Set(appr, ApprovalFlow.Run(...)); Notify(appr.approvalId)</code> — optionally wrapped in <b>IfError</b>.</li>\n<li>Use <b>Set</b> (global variables), <b>ClearCollect</b> (refresh the cached collection), and <b>Navigate</b> — chained with <b>;</b>. These are <b>behavior</b> functions with side effects, which are disallowed in property formulas like <code>Text</code>; only event/behavior properties (OnSelect) permit them.</li>\n</ol></details>\n\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Why can <code>Set</code> or <code>Patch</code> appear in <code>OnSelect</code> but not in a control's <code>Fill</code> property?</li>\n<li>What does a delegation warning mean, and what row count is evaluated locally when logic can't delegate?</li>\n<li>How do consuming apps receive fixes made to a component stored in a component library?</li>\n<li>What is the exact syntax to call a cloud flow from a button, and how do you read a value the flow returns?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><code>Fill</code> is a <b>property formula</b> that must be side-effect free; <code>Set</code>/<code>Patch</code> have <b>side effects</b> and are only permitted in <b>behavior formulas</b> such as <code>OnSelect</code>.</li>\n<li>The function/column can't be pushed to the data source, so Power Apps processes only the first <b>500</b> rows locally (configurable up to <b>2000</b>) — risking missed records on large sources.</li>\n<li>The library notifies them that an <b>update is available</b>; the maker chooses <b>Update</b> to import the new version — one fix propagates to every consuming app.</li>\n<li><code>FlowName.Run(arg1, arg2, ...)</code> in a behavior property; capture the result record (from <b>Respond to a Power App or flow</b>) with <code>Set(r, FlowName.Run(...))</code> then reference <code>r.fieldName</code>.</li>\n</ol></details>" },
  { id: "3.2", domain: 3, title: "Optimize and troubleshoot apps", ready: true,
    intro: "Diagnosing and fixing performance problems in Power Apps means reading the session event stream in Monitor, understanding delegation limits, and applying page-load patterns for both canvas and model-driven apps. This sub-skill is where you prove you can make a slow app fast.",
    html: "<h2>Notes</h2><h3>Monitor and browser debugging</h3><ul><li><b>Monitor</b> streams a live log of every event in an app session — data source calls, formula evaluation, screen navigation, control rendering — for <b>both canvas and model-driven apps</b>. Open it from Power Apps Studio (<b>Advanced tools &gt; Monitor</b>) or from the maker portal, then choose <b>Play published app</b> or connect to an existing session.</li><li>Each row shows the operation, <b>duration</b>, result/status, and a detailed pane (request URL, OData query, response payload, error message). Filter and search by category to isolate slow or failing operations.</li><li>Use Monitor to spot the classic offenders: a <b>delegation warning</b> silently truncating results, a chatty <b>OnStart</b> firing many calls, or a formula issuing repeated network requests. The <b>Network</b> and OData rows reveal the exact query sent to Dataverse.</li><li><b>Browser dev tools (F12)</b> complement Monitor: the Network tab shows raw HTTP calls, throttling (429) responses, and payload sizes; the Console surfaces script errors. For model-driven apps you can enable the <b>Performance / diagnostics</b> pages (append <code>&amp;perf=true</code> or <code>&amp;monitor=true</code> to the URL) and inspect <b>form load</b> timings.</li></ul><h3>Canvas performance — pre-load data and delegation</h3><ul><li><b>Delegation</b> pushes filtering, sorting, and aggregation to the data source so only matching rows return. When a function or predicate is <b>non-delegable</b>, Power Apps pulls only the first page of records (the <b>data row limit</b>, default <b>500</b>, configurable up to <b>2000</b>) and processes the rest locally — producing wrong results on large tables.</li><li>Delegation support depends on <b>both the data source and the function</b>. Dataverse and SQL Server are highly delegable; collections and imported Excel/static sources are held in memory (no delegation needed, but capped by the row limit). Non-delegable elements show a blue-dot <b>delegation warning</b> in Studio.</li><li>Reduce calls at startup: move heavy loads out of <b>OnStart</b>, use <b>concurrent</b> operations with <code>Concurrent()</code>, cache reference data into <b>collections</b> with <code>Collect</code>/<code>ClearCollect</code>, and set <b>OnStart</b> only for genuinely global setup. Prefer <b>OnVisible</b> per-screen loading so the first screen paints quickly.</li><li>Other patterns: limit the number of <b>data sources and connectors</b>, avoid repeated lookups by caching in <b>variables</b>, use <b>explicit column selection</b>, and turn on <b>delayed load</b> so screens hydrate on demand. Fewer controls and simpler formulas mean faster rendering.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Function / pattern</th><th>Delegable on Dataverse?</th><th>Guidance</th></tr></thead><tbody><tr><td><code>Filter</code>, <code>Search</code>, <code>LookUp</code></td><td>Yes (with delegable predicates)</td><td>Push the predicate to the server; watch for a mixed formula that breaks delegation</td></tr><tr><td><code>StartsWith</code>, <code>=</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&amp;&amp;</code>, <code>||</code></td><td>Yes</td><td>Delegable operators — safe on large tables</td></tr><tr><td><code>Sort</code> / <code>SortByColumns</code></td><td>Yes</td><td>Delegable on Dataverse; verify no non-delegable column</td></tr><tr><td><code>Sum</code>, <code>Average</code>, <code>Min</code>, <code>Max</code>, <code>CountRows</code></td><td>Partial</td><td>Aggregate delegation is limited — confirm the blue warning is absent</td></tr><tr><td>In-memory <b>collections</b> / imported Excel</td><td>N/A</td><td>No delegation needed, but still capped by the data row limit (500/2000)</td></tr></tbody></table></div><h3>Model-driven performance</h3><ul><li>Form load is dominated by the number of <b>controls, subgrids, and tabs</b> and by <b>synchronous</b> work. Reduce fields on the primary tab, set secondary tabs and subgrids to <b>load on demand</b> (not expanded by default), and minimize the number of business rules and OnLoad scripts that run synchronously.</li><li>Write efficient <b>client scripting</b>: register only necessary handlers, favor <b>asynchronous</b> <code>Xrm.WebApi</code> calls, request only needed columns with <b>$select</b>, and avoid unbounded retrieves. Bundle scripts into as few web resources as possible.</li><li>Use the <b>Performance / diagnostics</b> insight pages and Monitor to attribute time to form load vs. data retrieval vs. scripts. Server-side heavy logic belongs in <b>plug-ins/synchronous workflows only when required</b> — move long work to <b>async</b>.</li></ul><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li><b>Monitor works for canvas AND model-driven apps</b> and captures a full session event stream with per-operation durations — it is the first tool to reach for on a performance or logic bug.</li><li>Delegation limits: a non-delegable query returns only up to the <b>data row limit (default 500, max 2000)</b> — the fix is to make the query delegable, not to raise the limit.</li><li>Delegation depends on the <b>combination of data source and function</b>; Dataverse/SQL delegate, in-memory collections do not need it but are still capped by the row limit.</li><li>Move heavy work out of <b>OnStart</b>; use <code>Concurrent()</code>, cache with <code>ClearCollect</code>, and load per-screen with <b>OnVisible</b>.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>A canvas app filters a Dataverse table of 40,000 rows with <code>Filter(Accounts, StartsWith(Name, txtSearch.Text))</code> and users report missing records. What is happening and how do you fix it? <span class=\"check\">Decide, then check.</span></li><li>An app takes 15 seconds to reach the first screen; OnStart runs eight sequential <code>ClearCollect</code> calls against different tables. How do you cut the load time? <span class=\"check\">Decide, then check.</span></li><li>A model-driven main form is slow to open; it has 60 fields across one tab, three expanded subgrids, and two synchronous OnLoad scripts. What do you change? <span class=\"check\">Decide, then check.</span></li><li>You need to prove which operation is causing intermittent throttling errors in a published canvas app. Which tools and steps? <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li><code>StartsWith</code> is non-delegable in the way used here, so only the first 500 (or up to 2000) rows are searched locally and matches beyond that page are dropped. Rewrite to a delegable predicate (Dataverse delegates <code>StartsWith</code> — verify the blue delegation warning is gone) or narrow the dataset; do not simply raise the row limit, which only masks the truncation.</li><li>Wrap independent loads in <code>Concurrent()</code> so they run in parallel instead of sequentially, move non-essential reference data to <b>OnVisible</b> of the screens that need it, and keep OnStart minimal. Confirm the reduced call count and timing in Monitor.</li><li>Move most fields to secondary tabs, set subgrids and secondary tabs to <b>load on demand</b> rather than expanded, and make the OnLoad scripts <b>asynchronous</b> (or remove/consolidate them). Use the diagnostics page to confirm form-load time dropped.</li><li>Open <b>Monitor</b> against the published session and reproduce the issue; find the operation returning <b>429</b> with its duration and payload, then cross-check the raw request in browser <b>F12 Network</b>. Fix by reducing call frequency (caching, concurrency) or batching, not by retrying blindly.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>Which Power Apps tool streams a live, per-operation event log for both canvas and model-driven app sessions?</li><li>What is the default data row limit for non-delegable queries, and what is the maximum you can raise it to?</li><li>Name two techniques to reduce canvas app startup time.</li><li>Why is raising the delegation row limit a poor fix for a search that misses records on a large table?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li><b>Monitor</b> (Live monitor) — it works for both canvas and model-driven apps and shows each event with its duration, request, and result.</li><li>Default <b>500</b>; configurable up to <b>2000</b> in app settings. Beyond that, delegation is required.</li><li>Any two: run independent loads with <code>Concurrent()</code>; cache reference data into collections with <code>ClearCollect</code>; move loads from <b>OnStart</b> to per-screen <b>OnVisible</b>; reduce data sources/controls; select only needed columns.</li><li>Because the query is still non-delegable — it only searches the locally retrieved page. Raising the cap to 2000 just enlarges the page; rows beyond it are still ignored. The correct fix is to make the query <b>delegable</b>.</li></ol></details>" },
  { id: "4.1", domain: 4, title: "Apply business logic by using client scripting", ready: true,
    intro: "Model-driven client scripting uses JavaScript against the Client API object model to react to form and grid events, validate and shape data, and call the platform. Expect precise questions on execution context, per-event registration, Xrm.WebApi, and command-bar navigation to custom pages.",
    html: "<h2>Notes</h2>\n\n<h3>The Client API object model</h3>\n<ul>\n<li>Client scripting in model-driven apps runs <b>JavaScript</b> against the <b>Client API object model</b>, rooted at the global <code>Xrm</code> object. It reacts to user actions on forms and grids and can read/write form data, control the UI, and call the platform.</li>\n<li>The old <code>Xrm.Page</code> is <b>deprecated</b>. Get the current context from the <b>execution context</b> passed to your handler: <code>executionContext.getFormContext()</code> returns the <b>formContext</b> (a form or a grid on a form).</li>\n<li>Key collections hang off <code>formContext</code>: <b>data</b> (<code>formContext.data.entity.attributes</code>) and <b>ui</b> (<code>formContext.ui.controls</code>, tabs, sections). Attributes hold data and fire data events; controls are the visual representation.</li>\n<li>Common calls: <code>formContext.getAttribute(\"name\")</code> then <code>.getValue()</code>/<code>.setValue()</code>/<code>.setRequiredLevel()</code>; <code>formContext.getControl(\"name\")</code> then <code>.setVisible()</code>/<code>.setDisabled()</code>/<code>.setNotification()</code>.</li>\n<li>Global helpers: <b><code>Xrm.WebApi</code></b> (data operations), <b><code>Xrm.Navigation</code></b> (dialogs, forms, custom pages), <b><code>Xrm.Utility</code></b>, and <code>Xrm.App</code>. Register JavaScript as <b>web resources</b> (type <i>Script (JScript)</i>) inside a solution.</li>\n</ul>\n\n<h3>Registering event handlers — OnLoad, OnChange, OnSave</h3>\n<ul>\n<li>Handlers are registered in the <b>form designer</b> (Events tab / column properties), <b>not</b> auto-wired by naming. You add the <b>library</b> (web resource), name the <b>function</b>, and — critically — tick <b>\"Pass execution context as first parameter\"</b> so your function receives <code>executionContext</code>.</li>\n<li><b>OnLoad</b> — fires when the form loads; use it to initialize UI, set defaults, or attach further handlers. <b>OnSave</b> — fires when the record is saved; you can <b>cancel</b> the save with <code>executionContext.getEventArgs().preventDefault()</code> after validation.</li>\n<li><b>OnChange</b> — registered <b>per column (attribute)</b>; fires when a field value changes and loses focus. It does not fire while the user is still typing, and only fires if the value actually changed.</li>\n<li>Other events: <b>TabStateChange</b>, <b>OnRecordSelect</b>/grid events, <b>OnGridLoad</b>, and <b>OnOutputChange</b> for PCF. You can also add/remove handlers in code with <code>formContext.data.entity.addOnSave()</code>, <code>attribute.addOnChange()</code>, etc.</li>\n</ul>\n\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Event</th><th>Registered on</th><th>Fires when</th><th>Signature detail</th></tr></thead>\n<tbody>\n<tr><td><b>OnLoad</b></td><td>Form</td><td>Form finishes loading</td><td>Init UI / set defaults</td></tr>\n<tr><td><b>OnChange</b></td><td>A column (attribute)</td><td>Field value changed &amp; committed</td><td>Only on real change; not per keystroke</td></tr>\n<tr><td><b>OnSave</b></td><td>Form</td><td>Record is saved</td><td>Can cancel via <code>getEventArgs().preventDefault()</code></td></tr>\n</tbody>\n</table></div>\n\n<h3>Calling the Dataverse Web API from client script</h3>\n<ul>\n<li>Use <b><code>Xrm.WebApi</code></b> — it wraps the Dataverse <b>Web API</b> (OData v4) so you do not build raw HTTP. It is available <b>online</b> for both synchronous and asynchronous needs; the async methods return <b>Promises</b>.</li>\n<li>Core methods: <code>createRecord</code>, <code>retrieveRecord</code>, <code>retrieveMultipleRecords</code>, <code>updateRecord</code>, <code>deleteRecord</code>, and <code>execute</code>/<code>executeMultiple</code> for actions, functions, and custom APIs.</li>\n<li>Use the <b>entity set (logical collection) name</b> and <b>GUID</b>; shape results with OData query options — <code>?$select=name,revenue&amp;$filter=revenue gt 1000&amp;$expand=primarycontactid($select=fullname)&amp;$top=10</code>.</li>\n<li><code>Xrm.WebApi.online</code> supports batch and impersonation nuances; <code>Xrm.WebApi.offline</code> exists for mobile offline. Prefer this over jQuery/XMLHttpRequest — it handles auth, URL, and API version for you.</li>\n</ul>\n\n<h3>Commands, buttons, and navigation to custom pages</h3>\n<ul>\n<li><b>Ribbon/command bar buttons</b> are authored in the modern <b>command bar designer</b>, where a command's <b>action</b> can be <b>Power Fx</b> or a <b>JavaScript</b> function, and its <b>visibility</b> is driven by a <b>Power Fx formula</b> or classic <b>enable/display rules</b>. JavaScript commands receive parameters such as the <b>PrimaryControl</b> (the form/grid context).</li>\n<li><b>Navigation</b> uses <code>Xrm.Navigation</code>: <code>openForm</code> (open/create a record), <code>openAlertDialog</code>/<code>openConfirmDialog</code>, <code>openUrl</code>, and <code>navigateTo</code>.</li>\n<li><b>Custom pages</b> (canvas-based pages inside a model-driven app) are opened with <b><code>Xrm.Navigation.navigateTo</code></b>: pass <code>pageType: \"custom\"</code> and the page <code>name</code>, plus a <b>navigation target</b> (<code>1</code> = inline, <code>2</code> = dialog) with size/position. Pass <code>data</code> to the page and read it there via the <code>Param()</code> Power Fx function.</li>\n<li>Use <code>navigateTo</code> with <code>pageType: \"entityrecord\"</code>/<code>\"entitylist\"</code> for standard forms and views; the returned Promise lets you refresh the caller after a dialog closes.</li>\n</ul>\n\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li>Always call <code>executionContext.getFormContext()</code> — <code>Xrm.Page</code> is deprecated — and enable <b>\"Pass execution context as first parameter\"</b> when registering the handler.</li>\n<li><b>OnChange</b> is registered per <b>column</b>, fires only on a committed change; <b>OnSave</b> can <b>cancel</b> the save with <code>getEventArgs().preventDefault()</code>; <b>OnLoad</b> is form-level.</li>\n<li>Call Dataverse from the client with <b><code>Xrm.WebApi</code></b> (async methods return Promises), using entity-set names and OData <code>$select</code>/<code>$filter</code>/<code>$expand</code> — not raw XHR.</li>\n<li>Open a <b>custom page</b> with <b><code>Xrm.Navigation.navigateTo({pageType:\"custom\", name:...})</code></b>; command buttons run <b>Power Fx or JavaScript</b> with visibility driven by Power Fx.</li>\n</ul></div>\n\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>On the Account form, a field must become <b>required and visible</b> only when <i>Category = Preferred Customer</i>, re-evaluated as the user changes Category. Where do you register the logic and which API calls do you use?<span class=\"check\">Decide, then check.</span></li>\n<li>Before an Order is saved, you must block the save if the total exceeds the customer's credit limit, which lives on the related Account record. How do you validate and stop the save?<span class=\"check\">Decide, then check.</span></li>\n<li>A command bar button on the Case form should open a guided, canvas-style intake page as a centered dialog and pass the current Case ID to it. What do you configure?<span class=\"check\">Decide, then check.</span></li>\n<li>Your OnChange handler works when you test the function directly but <code>getFormContext()</code> throws \"not a function\" at runtime. What is the most likely cause?<span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>Register an <b>OnChange</b> handler on the <b>Category column</b> (and call it once from <b>OnLoad</b> for initial state). In it, use <code>formContext.getAttribute(\"category\").getValue()</code>, then <code>getAttribute(field).setRequiredLevel(\"required\"|\"none\")</code> and <code>getControl(field).setVisible(true|false)</code>.</li>\n<li>Register an <b>OnSave</b> handler. Retrieve the credit limit with <code>Xrm.WebApi.retrieveRecord</code> (or a cached lookup value), compare to the total, and if it fails call <code>executionContext.getEventArgs().preventDefault()</code> to cancel the save and show a form notification.</li>\n<li>In the <b>command bar designer</b>, add a button whose action calls <b><code>Xrm.Navigation.navigateTo</code></b> with <code>pageType:\"custom\"</code>, the page <code>name</code>, target <code>2</code> (dialog), centered position, and a <code>data</code> object carrying the Case ID from <b>PrimaryControl</b>; read it on the page with <code>Param()</code>.</li>\n<li>The handler was registered <b>without</b> \"Pass execution context as first parameter\", so <code>executionContext</code> is undefined. Enable that checkbox on the event registration so the context is passed in.</li>\n</ol></details>\n\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Which method gives you the form context inside a handler, and what deprecated object does it replace?</li>\n<li>On what object is an OnChange handler registered, and does it fire on every keystroke?</li>\n<li>Which Client API namespace do you use to create/retrieve Dataverse records from client script, and what do its async methods return?</li>\n<li>Which call opens a custom page, and how do you pass it a dialog target and data?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><code>executionContext.getFormContext()</code>; it replaces the deprecated <b><code>Xrm.Page</code></b>.</li>\n<li>On a specific <b>column (attribute)</b>; it fires only when the value actually changes and is committed (on blur), <b>not</b> per keystroke.</li>\n<li><b><code>Xrm.WebApi</code></b> (e.g. <code>createRecord</code>, <code>retrieveRecord</code>, <code>retrieveMultipleRecords</code>); its async methods return <b>Promises</b>.</li>\n<li><b><code>Xrm.Navigation.navigateTo</code></b> with <code>pageType:\"custom\"</code> and the page <code>name</code>; pass a navigation-options target of <b>2</b> (dialog) plus a <code>data</code> object read on the page via <code>Param()</code>.</li>\n</ol></details>" },
  { id: "4.2", domain: 4, title: "Create a Power Apps component framework (PCF) component", ready: true,
    intro: "The Power Apps component framework (PCF) lets pro developers build reusable code components in TypeScript that extend model-driven and canvas apps beyond what maker components allow. This guide covers the control lifecycle, the ControlManifest, the component interfaces, the CLI package/deploy path, and the framework features (Device, Utility, WebAPI) surfaced through context.",
    html: "<h2>Notes</h2><h3>What PCF is and when to use it</h3><ul><li><b>Code components</b> are built with the Power Apps component framework and run inside model-driven apps, canvas apps, portals/Power Pages, and dashboards. They render directly in the app's DOM rather than in an isolated iframe (unlike legacy web resources), so they perform well and can bind to fields (<b>field-type</b> controls) or whole datasets (<b>dataset-type</b> controls).</li><li>Choose PCF over an HTML web resource or a canvas maker component when you need custom visualization or interaction, reuse across many apps, access to the framework APIs (device, WebAPI, utility), and full TypeScript tooling. Code components require a <b>Power Apps per-app/per-user plan</b> or equivalent to run.</li><li>Tooling: the <b>Microsoft Power Platform CLI</b> (<b>pac</b>), Node.js/npm, and TypeScript. Canvas code components must be enabled per environment (the <b>Power Apps component framework</b> feature switch) before they can be consumed.</li></ul><h3>Lifecycle methods</h3><ul><li><b>init(context, notifyOutputChanged, state, container)</b> — called once when the component loads. Set up the control, cache <b>notifyOutputChanged</b> (the callback you invoke when an output value changes), read initial <b>context</b>, and add your root element to <b>container</b>. Async setup can begin here.</li><li><b>updateView(context)</b> — called every time data or context changes (bound value updates, resize, mode change). The framework passes a fresh <b>context</b>; re-render from <b>context.parameters</b>. Never cache values across calls — always read the current context.</li><li><b>getOutputs()</b> — called by the framework after you call <b>notifyOutputChanged</b>. Return an object whose properties match the output/bound properties in the manifest; those values flow back to the host (the bound column or canvas property).</li><li><b>destroy()</b> — called when the component is removed. Clean up: remove event listeners, cancel timers, and release resources to avoid memory leaks.</li></ul><h3>The ControlManifest</h3><ul><li><b>ControlManifest.Input.xml</b> declares the component's metadata and contract. Key elements: <b>&lt;control&gt;</b> (namespace, constructor, version, display/description keys), <b>&lt;property&gt;</b> (bound/input/output fields with <b>of-type</b> data types), <b>&lt;data-set&gt;</b> for dataset components, <b>&lt;resources&gt;</b> (code entry point plus css/resx/img), and <b>&lt;feature-usage&gt;</b> to opt into platform features.</li><li>To use <b>Device</b>, <b>WebAPI</b>, or <b>Utility</b> features you must declare them under <b>&lt;feature-usage&gt;</b> with <b>&lt;uses-feature name=\"WebAPI\" required=\"true\" /&gt;</b>; otherwise the corresponding <b>context</b> namespace is unavailable at runtime.</li><li>Build generates <b>ControlManifest.Input.xml</b> into typed metadata (<b>ManifestTypes.d.ts</b>) so <b>context.parameters</b> and outputs are strongly typed.</li></ul><h3>context and framework features</h3><ul><li><b>context.parameters</b> — the typed bound properties and dataset(s). <b>context.mode</b> — visibility, allocated width/height, and <b>context.mode.trackContainerResize(true)</b> to receive resize in updateView. <b>context.updatedProperties</b> lists what changed.</li><li><b>context.device</b> — <b>captureImage / captureAudio / captureVideo</b>, <b>getBarcodeValue</b>, <b>pickFile</b>, and <b>getCurrentPosition</b> (geolocation).</li><li><b>context.webAPI</b> — <b>createRecord, retrieveRecord, retrieveMultipleRecords, updateRecord, deleteRecord</b> against Dataverse. Available in model-driven apps; support in canvas is limited.</li><li><b>context.utils</b> — utility helpers such as <b>getEntityMetadata</b>; <b>context.navigation</b> — <b>openForm, openAlertDialog, openUrl</b>; <b>context.formatting</b> and <b>context.resources</b> for localized strings and images.</li></ul><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li><b>init</b> runs once; <b>updateView</b> runs on every change; <b>getOutputs</b> is pulled by the framework only after you call <b>notifyOutputChanged</b>; <b>destroy</b> cleans up.</li><li>Outputs are not pushed by returning from updateView — you must call <b>notifyOutputChanged()</b>, then the framework calls <b>getOutputs()</b>.</li><li>Any Device/WebAPI/Utility feature must be declared in <b>&lt;feature-usage&gt;</b> in the manifest or it won't be available in context.</li><li>Build/deploy path: <b>pac pcf init</b> &rarr; <b>npm install</b> &rarr; <b>npm run build</b> &rarr; test harness &rarr; <b>pac pcf push</b> (dev) or add to a <b>solution</b> and import (ALM).</li></ul></div><h3>Package, deploy, and consume</h3><ul><li>Scaffold with <b>pac pcf init --namespace &lt;ns&gt; --name &lt;name&gt; --template field|dataset</b>, then <b>npm install</b>. Build with <b>npm run build</b>; iterate locally in the <b>test harness</b> via <b>npm start</b>.</li><li>Quick dev deploy: <b>pac pcf push --publisher-prefix &lt;prefix&gt;</b> pushes the component to a connected Dataverse environment. For ALM, add the component to a <b>solution</b> (<b>pac solution add-reference</b>), build a managed solution, and import it to the target environment.</li><li>Consume: in a <b>model-driven app</b>, add the code component to a field or subgrid via <b>Component</b> on the form/column; in a <b>canvas app</b> (with the feature enabled), <b>Insert &gt; Get more components &gt; Code</b>, then bind properties.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Method</th><th>When called</th><th>Typical work</th></tr></thead><tbody><tr><td><b>init</b></td><td>Once, on load</td><td>Cache notifyOutputChanged, build DOM into container, read initial context</td></tr><tr><td><b>updateView</b></td><td>On every data/context change &amp; resize</td><td>Re-render from context.parameters</td></tr><tr><td><b>getOutputs</b></td><td>After notifyOutputChanged</td><td>Return output values to the host column/property</td></tr><tr><td><b>destroy</b></td><td>On removal</td><td>Remove listeners, cancel timers, free resources</td></tr></tbody></table></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>Your slider component must write the selected number back to a bound Dataverse column. Nothing you do in updateView updates the column. Decide what call is missing and which method the framework will invoke next. <span class=\"check\">Decide, then check.</span></li><li>A component needs to call the device camera to capture a photo and read a barcode, but <b>context.device</b> throws at runtime. Decide what is missing before the code will work. <span class=\"check\">Decide, then check.</span></li><li>You must ship a dataset visualization to production across three environments with source control and repeatable deployment, not just a dev push. Decide the packaging/deployment approach. <span class=\"check\">Decide, then check.</span></li><li>Your control leaks memory: each time it is added and removed from a form, timers keep firing. Decide where to fix it and what to do. <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>You never call <b>notifyOutputChanged()</b>. Cache the callback in <b>init</b> and invoke it when the value changes; the framework then calls <b>getOutputs()</b>, whose returned object (matching the manifest output property) is committed to the bound column.</li><li>The <b>Device</b> feature is not declared. Add <b>&lt;uses-feature name=\"Device\" required=\"true\" /&gt;</b> under <b>&lt;feature-usage&gt;</b> in ControlManifest.Input.xml so <b>context.device</b> (captureImage, getBarcodeValue) is provisioned.</li><li>Don't rely on <b>pac pcf push</b> (dev-time). Add the component to a <b>solution</b>, build it as a <b>managed</b> solution, keep it in source control, and deploy through ALM (import per environment or via pipelines/Build Tools).</li><li>Fix it in <b>destroy()</b> — clear timers (clearInterval), remove event listeners, and release references. <b>destroy</b> is the framework's cleanup hook called when the component is removed.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>Which lifecycle method is guaranteed to run exactly once and receives the <b>container</b> element and <b>notifyOutputChanged</b> callback?</li><li>After you call <b>notifyOutputChanged</b>, which method does the framework call, and what must its return value match?</li><li>Where do you declare that a component will use <b>context.webAPI</b>, and what happens if you omit it?</li><li>What is the difference between a <b>field</b> (property) component and a <b>dataset</b> component in the manifest?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li><b>init(context, notifyOutputChanged, state, container)</b> — called once on load; it is where you build the DOM into the container and cache the output callback.</li><li>The framework calls <b>getOutputs()</b>; its returned object's properties must match the <b>output/bound property</b> names and types declared in the manifest, and those values flow back to the host.</li><li>In <b>ControlManifest.Input.xml</b> under <b>&lt;feature-usage&gt;</b> via <b>&lt;uses-feature name=\"WebAPI\" required=\"true\" /&gt;</b>. If omitted, <b>context.webAPI</b> is not provisioned and calling it fails at runtime.</li><li>A <b>field</b> component binds to a single column via <b>&lt;property&gt;</b> (of-type data types); a <b>dataset</b> component binds to a collection of records via <b>&lt;data-set&gt;</b> and is used on views/subgrids or bound to a canvas table/collection.</li></ol></details>" },
  { id: "5.1", domain: 5, title: "Create a Dataverse plug-in", ready: true,
    intro: "Plug-ins are the core of PL-400's largest domain: server-side .NET code that runs inside the Dataverse event pipeline to extend business logic. Expect precise questions on pipeline stage, sync vs async, images, the execution context, and when a custom API is the right message to build.",
    html: "<h2>Notes</h2>\n\n<h3>What a plug-in is</h3>\n<ul>\n<li>A plug-in is a compiled .NET class that implements the <code>IPlugin</code> interface and its single method <code>void Execute(IServiceProvider serviceProvider)</code>. Dataverse invokes it when a registered <b>message</b> (Create, Update, Delete, a custom API, etc.) runs against a table.</li>\n<li>Plug-ins must be <b>stateless</b>. Dataverse caches and reuses the <code>IPlugin</code> instance across invocations and threads, so instance/member fields are shared and are not thread-safe. Put all per-call state in local variables inside <code>Execute</code>.</li>\n<li>Configuration is passed to the class <b>constructor</b>: <code>public MyPlugin(string unsecureConfig, string secureConfig)</code>. The <b>unsecure</b> string is readable by anyone who can view the step; the <b>secure</b> string is protected and not exported in solutions the same way.</li>\n<li>Online, plug-ins run in the <b>sandbox</b> (partial-trust) isolation mode — no file system, registry, or event log access; only outbound HTTP/HTTPS network calls are allowed. There is a <b>2-minute (120 second)</b> execution time limit per invocation.</li>\n<li>Register the assembly and steps with the <b>Plug-in Registration Tool (PRT)</b>, obtained from NuGet (<code>Microsoft.CrmSdk.XrmTooling.PluginRegistrationTool</code>) or via <code>pac tool prt</code>. The assembly must be <b>strong-name signed</b>.</li>\n</ul>\n\n<h3>The event execution pipeline</h3>\n<ul>\n<li>Every data operation is a <b>message</b> that flows through a processing pipeline of ordered <b>stages</b>. You register a step on a message + table + stage.</li>\n<li><b>Pre-validation (stage 10)</b> runs <b>before</b> the main operation, <b>outside</b> the database transaction, and <b>before</b> security/privilege checks. It runs even if the calling user lacks privilege for the operation, making it the right place for early validation and cancellation, and for logic that must span a cascade.</li>\n<li><b>Pre-operation (stage 20)</b> runs <b>inside</b> the transaction, after validation, <b>before</b> the change is written. Use it to modify the <code>Target</code> before it is saved (values set here are persisted without a second update).</li>\n<li><b>Main operation (stage 30)</b> is the core system write. It is reserved — you cannot register custom sync/async steps here except as the single main-operation plug-in of a <b>custom API</b>.</li>\n<li><b>Post-operation (stage 40/50)</b> runs <b>inside</b> the transaction, after the change is written. Use it for follow-on work (create related rows, call external systems) and to read the committed result, e.g. the new record's id.</li>\n<li>Throwing an <code>InvalidPluginExecutionException</code> in a <b>synchronous</b> in-transaction stage <b>rolls back</b> the whole operation and surfaces your message to the user.</li>\n</ul>\n\n<h3>Synchronous vs asynchronous</h3>\n<ul>\n<li><b>Synchronous</b> steps run immediately, in-line, and <b>block</b> the caller until they finish — the user waits, and the operation succeeds or fails as a unit. Use for validation and for changes the user must see reflected instantly.</li>\n<li><b>Asynchronous</b> steps are queued as <b>system jobs</b> (asyncoperation rows) and run in the background by the async service after the operation completes. They do <b>not</b> block the user and cannot alter the response. Async is only valid in the <b>post-operation</b> stage.</li>\n<li>Prefer async for long-running, non-critical, or external-integration work; keep sync plug-ins fast because they count against the 2-minute limit and delay the user.</li>\n</ul>\n\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Stage</th><th>In transaction?</th><th>Security check done?</th><th>Sync/async</th><th>Typical use</th></tr></thead>\n<tbody>\n<tr><td><b>Pre-validation (10)</b></td><td>No</td><td>No (runs before)</td><td>Sync only</td><td>Early validation / cancel; cross-cascade logic</td></tr>\n<tr><td><b>Pre-operation (20)</b></td><td>Yes</td><td>Yes</td><td>Sync only</td><td>Modify Target before the write</td></tr>\n<tr><td><b>Main operation (30)</b></td><td>Yes</td><td>Yes</td><td>Reserved</td><td>Custom API main plug-in only</td></tr>\n<tr><td><b>Post-operation (40)</b></td><td>Yes</td><td>Yes</td><td>Sync or async</td><td>Follow-on writes, integrations, read result</td></tr>\n</tbody>\n</table></div>\n\n<h3>Execution context, Target, and images</h3>\n<ul>\n<li>Retrieve services from the provider inside <code>Execute</code>:\n<ul>\n<li><code>IPluginExecutionContext context = (IPluginExecutionContext)serviceProvider.GetService(typeof(IPluginExecutionContext));</code></li>\n<li><code>IOrganizationServiceFactory factory = (IOrganizationServiceFactory)serviceProvider.GetService(typeof(IOrganizationServiceFactory));</code></li>\n<li><code>IOrganizationService service = factory.CreateOrganizationService(context.UserId);</code></li>\n<li><code>ITracingService tracing = (ITracingService)serviceProvider.GetService(typeof(ITracingService));</code></li>\n</ul></li>\n<li>The context exposes <b>InputParameters</b>, <b>OutputParameters</b>, <b>SharedVariables</b>, <b>PreEntityImages</b>, <b>PostEntityImages</b>, plus <code>MessageName</code>, <code>PrimaryEntityName</code>, <code>Stage</code>, <code>Mode</code> (0 sync / 1 async), <code>Depth</code>, <code>UserId</code>, and <code>InitiatingUserId</code>.</li>\n<li><b>Target</b>: <code>context.InputParameters[\"Target\"]</code> is an <code>Entity</code> for Create/Update and an <code>EntityReference</code> for Delete. On <b>Update</b> the Target contains <b>only the changed attributes</b> — read unchanged values from a pre-image.</li>\n<li><b>OutputParameters</b>: on Create, <code>context.OutputParameters[\"id\"]</code> holds the new record's <code>Guid</code> (available in post-operation).</li>\n<li><b>Images</b> capture a snapshot of the row, registered per step by <b>alias</b> and attribute list:\n<ul>\n<li><b>Pre-image</b> = values <b>before</b> the operation. Available on <b>Update</b> and <b>Delete</b>; not Create (nothing exists yet).</li>\n<li><b>Post-image</b> = values <b>after</b> the operation. Available on <b>Create</b> and <b>Update</b>; not Delete (nothing remains).</li>\n</ul>\nAccess as <code>Entity pre = context.PreEntityImages[\"PreImage\"];</code>. Register only the columns you need — images add payload and cost.</li>\n<li><b>SharedVariables</b> pass data between a pre-stage and post-stage of the same operation (or to a child pipeline via <code>ParentContext</code>).</li>\n</ul>\n\n<h3>Organization service operations &amp; performance</h3>\n<ul>\n<li><code>IOrganizationService</code> is the .NET SDK gateway: <b>Create</b>, <b>Retrieve</b>, <b>RetrieveMultiple</b> (with <code>QueryExpression</code> or <code>FetchExpression</code> + <code>ColumnSet</code>), <b>Update</b>, <b>Delete</b>, <b>Associate</b>/<b>Disassociate</b>, and <b>Execute</b> for any other <code>OrganizationRequest</code> (e.g. <code>WhoAmIRequest</code>, <code>UpsertRequest</code>).</li>\n<li>Prefer <code>CreateOrganizationService(context.UserId)</code> to run as the initiating user (respects security). Pass <code>null</code> to run as the SYSTEM user.</li>\n<li><b>Performance rules the exam loves:</b> register on specific <b>filtering attributes</b> so an Update step fires only when those columns change; request only needed columns in a <code>ColumnSet</code>; reuse image data instead of re-retrieving; keep sync plug-ins under the 2-minute limit; move heavy/external work to <b>async</b>.</li>\n<li><b>Infinite-loop guard:</b> <code>context.Depth</code> increments each time one plug-in's operation triggers another; Dataverse aborts at depth <b>8</b>. Guard against recursion (check Depth, or avoid updating the same row your step is registered on).</li>\n<li>Use <code>ITracingService.Trace(...)</code> for diagnostics; trace output appears in the plug-in trace log and in the error dialog when an exception is thrown.</li>\n</ul>\n\n<h3>Custom API messages &amp; business events</h3>\n<ul>\n<li>A <b>custom API</b> is a code-first way to define your <b>own message</b> (verb) in Dataverse — a modern, solution-aware alternative to legacy custom process actions. You author the request/response contract and back it with a plug-in.</li>\n<li>Key custom API settings: <b>Unique Name</b>, <b>Binding Type</b> (Global, Entity, or EntityCollection) and Bound Entity, <b>Is Function</b> (true = callable via GET, no side effects; false = POST action), <b>Is Private</b>, <b>Allowed Custom Processing Step Type</b> (whether others can add steps), and <b>Execute Privilege Name</b> to gate who can call it.</li>\n<li>Define <b>Custom API Request Parameters</b> and <b>Response Properties</b> for the contract. The main logic is a plug-in registered as the API's <b>main operation</b>; it is invocable from the Web API and the Organization service like any built-in message.</li>\n<li><b>Dataverse business events</b> represent a meaningful change in state, surfaced through the <b>event catalog</b>. They let low-code and pro-code consumers (Power Automate via the catalog, or plug-in steps) react to events, and you can extend the platform with <b>custom events</b> and connect <b>external events</b> — a way to decouple producers and consumers of business signals.</li>\n</ul>\n\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li><b>Pre-validation</b> is the only stage <b>outside</b> the transaction and <b>before</b> security checks — use it for early validation/cancel; <b>pre-operation</b> modifies the Target before the write; <b>post-operation</b> reads the committed result.</li>\n<li><b>Async runs post-operation only</b>, as background system jobs; sync blocks the user and can roll back via <code>InvalidPluginExecutionException</code>.</li>\n<li><b>Pre-images</b> exist on Update/Delete; <b>post-images</b> exist on Create/Update. On Update the Target has only changed columns — get the rest from a pre-image.</li>\n<li>Plug-ins are <b>stateless</b> (instance reused across threads); tune with <b>filtering attributes</b>, minimal <code>ColumnSet</code>, images over re-retrieval, and watch <code>context.Depth</code> (aborts at 8).</li>\n<li>Build a new <b>message</b> with a <b>custom API</b> (code-first, solution-aware); register everything with the <b>Plug-in Registration Tool</b>.</li>\n</ul></div>\n\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>You must block an Account update when a proposed <i>Credit Limit</i> exceeds an approved cap, and show the user a clear error. Which stage, mode, and exception do you use?<span class=\"check\">Decide, then check.</span></li>\n<li>On Update of a Case you need both the old and new value of <i>Status</i> to write an audit note, but the Target only holds changed fields. How do you get the previous value, and where do you register?<span class=\"check\">Decide, then check.</span></li>\n<li>After a new Order is created, you must call an external billing REST service that can take several seconds and must not delay the user. What execution mode and stage?<span class=\"check\">Decide, then check.</span></li>\n<li>Multiple apps need to invoke a reusable \"RecalculatePricing\" operation with typed inputs/outputs, callable from Web API and server code, restricted to authorized callers. What do you build?<span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>A <b>synchronous</b> step in <b>pre-validation</b> (or pre-operation) on the Update message, filtered to the Credit Limit attribute; throw <code>InvalidPluginExecutionException(\"...\")</code> to cancel the operation and display the message.</li>\n<li>Register a <b>pre-image</b> (alias e.g. \"PreImage\") on the step including the Status column, and read <code>context.PreEntityImages[\"PreImage\"]</code> for the old value and <code>Target</code> for the new — a <b>post-operation</b> step so the change is committed.</li>\n<li><b>Asynchronous</b>, in <b>post-operation</b> — it runs as a background system job after the create commits, so the user is not blocked and external latency does not affect the transaction.</li>\n<li>A <b>custom API</b> (global binding) with request parameters and response properties, backed by a plug-in as the main operation, secured with an <b>Execute Privilege</b>; callable as a message from the Web API and Organization service.</li>\n</ol></details>\n\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Which pipeline stage runs outside the database transaction and before security checks, and what is it best for?</li>\n<li>On an Update message, which images are available, and why can't a Create step have a pre-image?</li>\n<li>In which stage can asynchronous plug-in steps be registered, and what backs them at runtime?</li>\n<li>Why must plug-in classes be stateless, and what does <code>context.Depth</code> protect against?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><b>Pre-validation (stage 10)</b> — outside the transaction and before privilege checks; best for early validation and cancelling an operation, including logic that must run even when the user lacks privilege.</li>\n<li>Both a <b>pre-image</b> (values before) and a <b>post-image</b> (values after). A Create has no pre-image because the row does not exist before the operation.</li>\n<li>Only the <b>post-operation</b> stage. Async steps run as background <b>system jobs</b> (asyncoperation) executed by the async service.</li>\n<li>Because Dataverse <b>caches and reuses the plug-in instance across invocations and threads</b>, so shared fields are not thread-safe — keep state in locals. <code>context.Depth</code> guards against <b>infinite recursion</b>, aborting when a plug-in-triggered chain reaches depth 8.</li>\n</ol></details>" },
  { id: "5.2", domain: 5, title: "Create a custom connector", ready: true,
    intro: "Custom connectors wrap any REST API as a first-class Power Platform action set that canvas apps and cloud flows can call. You describe the API with an OpenAPI (Swagger) definition, secure it with an authentication type, and shape its behavior with x-ms-* extensions, policy templates, and data transforms.",
    html: "<h2>Notes</h2><h3>What a custom connector is and how you build one</h3><ul><li>A <b>custom connector</b> is a wrapper around a REST (or SOAP) API that lets Power Apps, Power Automate, and Azure Logic Apps call it as a set of typed <b>actions</b> and <b>triggers</b>, with authentication handled for you.</li><li>The connector definition has four logical parts you configure in the wizard: <b>General</b> (host, base URL, description), <b>Security</b> (authentication type), <b>Definition</b> (actions, triggers, request/response schemas), and <b>Test</b>.</li><li>Four ways to create one: <b>import an OpenAPI file</b>, <b>import an OpenAPI URL</b>, <b>import a Postman collection</b>, or <b>start from blank</b> and define operations by hand.</li><li>Lifecycle: <b>build</b> the definition, <b>secure</b> it, <b>describe/test</b> it, then <b>share</b> within the environment and optionally <b>certify</b> it with Microsoft to publish it to all tenants.</li><li>A connector is an environment component and ships in a <b>solution</b> for ALM; connection references bind it to a specific connection at deploy time.</li></ul><h3>OpenAPI (Swagger) definition</h3><ul><li>The connector is defined by an <b>OpenAPI</b> document (formerly Swagger, the wizard still says \"Swagger\"), typically <b>OpenAPI 2.0</b>. It describes the host, <code>basePath</code>, <code>paths</code>, operations, parameters, and response <code>definitions</code> (schemas).</li><li>Each operation needs a unique <b>operationId</b>, a <b>summary</b> and <b>description</b> (these become the action name and help text the maker sees), and typed parameters and responses so app makers get IntelliSense.</li><li>You can edit the raw document with the <b>Swagger editor</b> toggle in the wizard, or import a file/URL your API already exposes.</li><li>Provide a <b>200 response schema</b> so downstream steps see strongly typed output tokens instead of raw JSON.</li></ul><h3>Authentication types</h3><ul><li><b>No authentication</b> — anonymous/public APIs.</li><li><b>Basic authentication</b> — username and password; the caller supplies credentials when creating the connection.</li><li><b>API Key</b> — a key passed in a header or query string; you name the key and its location, the maker supplies the value.</li><li><b>OAuth 2.0</b> — for identity-provider-backed APIs (Microsoft Entra ID, generic OAuth, etc.); you register an app and supply <b>client id</b>, <b>client secret</b>, <b>authorization URL</b>, <b>token URL</b>, <b>refresh URL</b>, and <b>scope</b>. Use this for Azure services and Azure Functions secured by Entra ID.</li><li>The security type is set once per connector and drives how the connection prompts the user.</li></ul><h3>Policy templates and transforming data</h3><ul><li><b>Policy templates</b> modify the connector's behavior at <b>runtime</b> without changing the backend API — e.g. <b>Set HTTP header</b>, <b>Route request</b>, <b>Set query string parameter</b>, <b>Convert array to object</b>, <b>Set host URL</b>, <b>Update string using Regular Expression</b>.</li><li>Policies can reference <b>connection parameters</b> and support <b>environment variables</b>, so a value (like a host) differs per environment without editing the definition.</li><li>Transform data by editing schemas, applying policies (e.g. array-to-object), or using <b>x-ms-*</b> extensions to reshape what the maker sees vs. what the API returns.</li></ul><h3>Extending an OpenAPI definition with x-ms-* extensions</h3><ul><li><b>x-ms-summary</b> — friendly display name for a parameter/property.</li><li><b>x-ms-visibility</b> — <code>important</code>, <code>advanced</code>, or <code>internal</code> to control prominence in the maker UI.</li><li><b>x-ms-dynamic-values</b> / <b>x-ms-dynamic-schema</b> — populate dropdowns and reshape schemas by calling back into the API at design time.</li><li><b>x-ms-trigger</b> and <b>x-ms-trigger-hint</b> — mark an operation as a <b>polling</b> or <b>webhook</b> trigger.</li><li><b>x-ms-notification-content</b> — payload schema for webhook triggers.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Auth type</th><th>You configure</th><th>Best for</th></tr></thead><tbody><tr><td>No auth</td><td>Nothing</td><td>Public/anonymous APIs</td></tr><tr><td>Basic</td><td>Username &amp; password</td><td>Simple internal APIs</td></tr><tr><td>API Key</td><td>Key name &amp; location (header/query)</td><td>Key-secured SaaS/REST APIs</td></tr><tr><td>OAuth 2.0</td><td>Client id/secret, auth/token/refresh URLs, scope</td><td>Entra-secured Azure services &amp; Functions</td></tr></tbody></table></div><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li>The connector is described by an <b>OpenAPI 2.0 (Swagger)</b> document; you can import a file, a URL, or a <b>Postman collection</b>, or start blank.</li><li>Auth is chosen once from <b>No auth, Basic, API Key, or OAuth 2.0</b>; Azure Functions/APIs behind Entra ID use <b>OAuth 2.0</b> with client id/secret and token URLs.</li><li><b>Policy templates</b> change behavior at <b>runtime</b> (set header, route request, convert array to object) and can read <b>environment variables</b> — no backend change.</li><li>Extend/transform with <b>x-ms-*</b> extensions: <b>x-ms-summary</b>, <b>x-ms-visibility</b>, <b>x-ms-dynamic-values/schema</b>, <b>x-ms-trigger</b>.</li><li>Ship the connector inside a <b>solution</b> with connection references for ALM across environments.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>Your team already publishes an OpenAPI/Swagger URL for an internal REST API secured by an API key in a header. You need makers to call it from a cloud flow next week. How do you create the connector and set up auth? <span class=\"check\">Decide, then check.</span></li><li>An Azure Function is protected by Microsoft Entra ID app registration. The connector must let each maker sign in as themselves. Which authentication type and what values do you supply? <span class=\"check\">Decide, then check.</span></li><li>The backend returns the same JSON everywhere, but the <b>host</b> differs between Dev, Test, and Prod. You must not edit the connector per environment. What mechanism handles this? <span class=\"check\">Decide, then check.</span></li><li>Makers complain an action's dropdown for \"project\" is a free-text box and the response tokens are untyped. What do you change in the definition? <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>Create the custom connector by <b>importing the OpenAPI URL</b>; on the Security tab choose <b>API Key</b>, name the key, and set its location to <b>header</b>. Makers supply the key value when they create the connection. Test the action, then ship the connector in a solution.</li><li>Choose <b>OAuth 2.0</b> with the <b>Microsoft Entra ID</b> identity provider. Supply the app registration's <b>client id</b> and <b>client secret</b>, the <b>resource URL/scope</b>, and the authorization/token URLs. Each user consents and connects as themselves.</li><li>Use a <b>policy template</b> (e.g. <b>Set host URL</b> / Route request) whose value comes from an <b>environment variable</b>, or bind the host via a connection parameter. The single definition then resolves the correct host per environment at runtime.</li><li>Extend the OpenAPI definition: add <b>x-ms-dynamic-values</b> (or x-ms-dynamic-schema) so the \"project\" parameter is a dropdown populated by an API call, add <b>x-ms-summary</b> for a friendly label, and define a typed <b>200 response schema</b> so downstream steps get strongly typed tokens.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>Which document format describes a custom connector's operations, and what are the four ways to start creating one?</li><li>An API is secured by a bearer token issued by Entra ID. Which authentication type do you pick, and name two values you must provide.</li><li>What is a policy template, at what point does it act, and give two examples.</li><li>Which x-ms-* extension turns a plain text parameter into a design-time dropdown, and which marks an operation as a trigger?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li>An <b>OpenAPI 2.0 (Swagger)</b> definition. Start by importing an <b>OpenAPI file</b>, importing an <b>OpenAPI URL</b>, importing a <b>Postman collection</b>, or creating <b>from blank</b>.</li><li><b>OAuth 2.0</b>. Provide any two of: <b>client id</b>, <b>client secret</b>, <b>authorization URL</b>, <b>token URL</b>, <b>refresh URL</b>, <b>scope/resource</b>.</li><li>A <b>policy template</b> is a reusable rule that modifies connector behavior at <b>runtime</b> without changing the backend API. Examples: <b>Set HTTP header</b>, <b>Route request</b>, <b>Set query string parameter</b>, <b>Convert array to object</b>. It can read <b>environment variables</b>.</li><li><b>x-ms-dynamic-values</b> (or x-ms-dynamic-schema) populates a dropdown at design time; <b>x-ms-trigger</b> marks an operation as a polling or webhook trigger.</li></ol></details>" },
  { id: "5.3", domain: 5, title: "Use Power Platform APIs", ready: true,
    intro: "Dataverse exposes two programmatic surfaces over the same data and business logic: the OData v4 Web API for any HTTP-capable client, and the .NET SDK's Organization service for C# code. This sub-skill is about choosing between them, authenticating with OAuth, honoring service protection limits, and moving data efficiently with concurrency, transactions, and bulk operations.",
    html: "<h2>Notes</h2>\n<h3>Dataverse Web API (OData v4)</h3>\n<ul>\n<li>The Web API is a <b>RESTful, OData v4</b> endpoint at <code>[Organization URI]/api/data/v9.2/</code>. It is the platform-agnostic choice: any language or tool that can send HTTP works (JavaScript, Python, Postman), and it is what <code>Xrm.WebApi</code> calls from client script.</li>\n<li>CRUD maps to HTTP verbs: <b>GET</b> retrieve/query, <b>POST</b> create, <b>PATCH</b> update (and upsert), <b>DELETE</b> delete. Use the entity <b>set name</b> (plural, e.g. <code>/accounts</code>) and address a row by GUID in parentheses: <code>/accounts(&lt;id&gt;)</code>.</li>\n<li>OData system query options shape the response: <code>$select</code> (return only needed columns), <code>$filter</code>, <code>$orderby</code>, <code>$top</code>, and <code>$expand</code> to include related rows across a relationship (a server-side join, avoiding extra round trips).</li>\n<li>Key request/response headers: <code>OData-MaxVersion: 4.0</code> and <code>OData-Version: 4.0</code> are required; <code>Prefer: return=representation</code> returns the created/updated row in the response so you avoid a follow-up GET; <code>Prefer: odata.include-annotations=\"*\"</code> returns formatted values and lookup logical names; <code>If-Match</code> / <code>If-None-Match</code> drive concurrency and upsert.</li>\n<li>Batch multiple operations in one HTTP request with <b>$batch</b>. Operations grouped in a <b>changeset</b> execute as a single transaction (all-or-nothing); operations outside a changeset are non-transactional. Batching also reduces round trips against the service protection limits.</li>\n<li>Custom API and out-of-box <b>actions</b> (POST) and <b>functions</b> (GET) are callable over the Web API, bound or unbound.</li>\n</ul>\n<h3>Organization service (SDK for .NET)</h3>\n<ul>\n<li>Server-side C# (plug-ins, custom workflow activities, console/worker apps) uses the <b>Organization service</b> via the <code>IOrganizationService</code> interface: the core methods are <code>Create</code>, <code>Retrieve</code>, <code>RetrieveMultiple</code>, <code>Update</code>, <code>Delete</code>, <code>Associate</code>, <code>Disassociate</code>, and the general-purpose <code>Execute</code>.</li>\n<li>For external client apps, connect with <b>ServiceClient</b> (namespace <code>Microsoft.PowerPlatform.Dataverse.Client</code>), the current replacement for the legacy <code>CrmServiceClient</code>. It implements <code>IOrganizationService</code> and is built on OAuth.</li>\n<li><code>Execute</code> takes a strongly typed <b>message request</b> and returns a response — e.g. <code>WhoAmIRequest</code>, <code>CreateRequest</code>, <code>RetrieveMultipleRequest</code>, <code>ExecuteMultipleRequest</code>, <code>ExecuteTransactionRequest</code>, <code>UpsertRequest</code>. This is how you reach messages that have no dedicated method.</li>\n<li>Data access can be <b>early-bound</b> (generated proxy classes via <code>pac modelbuilder</code>, compile-time checking) or <b>late-bound</b> (the generic <code>Entity</code> with string attribute names). Queries use <b>QueryExpression</b> (typed, programmatic) or <b>FetchXML</b> (XML, supports aggregation and is portable).</li>\n</ul>\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Aspect</th><th>Web API (OData v4)</th><th>Organization service (.NET SDK)</th></tr></thead>\n<tbody>\n<tr><td>Protocol</td><td>REST / OData v4 over HTTP</td><td>.NET, <code>IOrganizationService</code> / <code>ServiceClient</code></td></tr>\n<tr><td>Best for</td><td>Any HTTP client, JS/TS, other languages, client script</td><td>Server-side C#: plug-ins, custom APIs, .NET integration apps</td></tr>\n<tr><td>CRUD</td><td>GET / POST / PATCH / DELETE</td><td>Create / Retrieve / Update / Delete / Execute</td></tr>\n<tr><td>Query</td><td>OData options ($select, $filter, $expand)</td><td>QueryExpression, FetchXML, LINQ (early-bound)</td></tr>\n<tr><td>Transactions</td><td>$batch changeset</td><td><code>ExecuteTransactionRequest</code></td></tr>\n<tr><td>Auth</td><td>OAuth 2.0 Bearer token (Microsoft Entra)</td><td>OAuth via connection string / MSAL</td></tr>\n</tbody>\n</table></div>\n<h3>OAuth authentication</h3>\n<ul>\n<li>All access to Dataverse is <b>OAuth 2.0</b> against <b>Microsoft Entra ID</b>. Legacy Office365 / basic (username-password only) auth is deprecated — never hard-code that pattern for new work.</li>\n<li>Register an <b>app registration</b> in Entra, grant the <b>Dynamics CRM</b> API permission (<code>user_impersonation</code>), and acquire tokens with the <b>MSAL</b> library. The resource/scope is the environment URL, e.g. <code>https://org.crm.dynamics.com/.default</code>.</li>\n<li><b>Server-to-server (S2S)</b> / daemon apps use the client-credentials flow (client ID + <b>client secret or certificate</b>, certificate preferred) with no signed-in user. You must create a corresponding <b>application user</b> in the environment and assign it a security role, or every call is denied.</li>\n<li><code>ServiceClient</code> accepts a connection string, e.g. <code>AuthType=ClientSecret;Url=...;ClientId=...;ClientSecret=...</code>, and manages token acquisition and refresh for you. Web API callers must attach the token themselves as an <code>Authorization: Bearer</code> header.</li>\n</ul>\n<h3>Service protection API limits &amp; retry</h3>\n<ul>\n<li>Dataverse enforces per-user, per-server <b>service protection limits</b> to keep one caller from starving others. Exceeding a limit returns HTTP <b>429 (Too Many Requests)</b> with a <code>Retry-After</code> header telling you how many seconds to wait.</li>\n<li>Three limits are measured over a <b>5-minute sliding window</b> per user, per web server: number of requests, total execution time, and number of concurrent requests (see table).</li>\n<li><b>Retry policy:</b> on a 429, wait the <code>Retry-After</code> duration then retry with exponential backoff — do not hammer immediately. <code>ServiceClient</code> retries automatically on service protection faults; a raw Web API client must implement the retry itself.</li>\n<li>These limits are about <b>protecting the service</b>, distinct from entitlement/API request allocations (licensing). You cannot request an exemption for normal app traffic; design within them.</li>\n</ul>\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Service protection limit (per user, per server, 5-min window)</th><th>Threshold</th></tr></thead>\n<tbody>\n<tr><td>Number of requests</td><td>6,000</td></tr>\n<tr><td>Combined execution (server) time</td><td>20 minutes (1,200 seconds)</td></tr>\n<tr><td>Number of concurrent requests</td><td>52 (guaranteed at least 6; often higher)</td></tr>\n<tr><td>Signal when exceeded</td><td>HTTP 429 + <code>Retry-After</code> header</td></tr>\n</tbody>\n</table></div>\n<h3>Performance, concurrency, transactions &amp; bulk</h3>\n<ul>\n<li><b>Bulk operations:</b> <code>ExecuteMultipleRequest</code> sends many message requests in one round trip; set <code>ContinueOnError=true</code> to keep going past failures and <code>ReturnResponses</code> to collect results. The newer <b>CreateMultiple / UpdateMultiple</b> messages (and <code>UpsertMultiple</code>) are optimized bulk messages that are far faster for high-volume writes and are the preferred pattern for elastic tables.</li>\n<li><b>Transactions:</b> <code>ExecuteTransactionRequest</code> runs a set of operations as a single atomic unit — all succeed or all roll back. In the Web API the equivalent is a <b>$batch changeset</b>. Note <code>ExecuteMultiple</code> is <em>not</em> transactional; it is for throughput, not atomicity.</li>\n<li><b>Concurrency (optimistic):</b> Dataverse uses row versions to prevent lost updates. In the SDK set <code>ConcurrencyBehavior.IfRowVersionMatches</code> and supply the entity's <code>RowVersion</code>; in the Web API send an <code>If-Match: \"&lt;etag&gt;\"</code> header. A mismatch fails with a concurrency error instead of silently overwriting.</li>\n<li><b>Throughput tuning:</b> run parallel requests to raise effective throughput, but stay under the concurrency limit; batch related writes; select only needed columns; and always honor 429 backoff. Combining <code>CreateMultiple</code> with a controlled degree of parallelism and retry gives the best sustained bulk load.</li>\n</ul>\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li><b>429 + Retry-After = service protection limit.</b> The correct response is retry with backoff after the <code>Retry-After</code> delay — not raising a support case or splitting into more parallel calls.</li>\n<li><code>ExecuteMultipleRequest</code> is for <b>throughput</b> (optionally ContinueOnError); <code>ExecuteTransactionRequest</code> / a <b>$batch changeset</b> is for <b>all-or-nothing atomicity</b>. Don't confuse the two.</li>\n<li>For high-volume writes prefer <b>CreateMultiple / UpdateMultiple</b> over looping single Create calls.</li>\n<li>S2S apps need both an Entra <b>app registration</b> and a matching <b>application user with a security role</b> in the environment.</li>\n<li>Optimistic concurrency = SDK <code>RowVersion</code> + <code>ConcurrencyBehavior.IfRowVersionMatches</code>, or Web API <code>If-Match</code> ETag.</li>\n</ul></div>\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>A Python service running in Azure must read and write Dataverse rows. There is no signed-in user. What API surface and auth flow do you choose, and what must exist in the environment? <span class=\"check\">Decide, then check.</span></li>\n<li>A nightly job must upsert 50,000 rows. Some rows may fail validation, but a failure of one row must not abort the rest, and you want maximum throughput. Which message(s) and options? <span class=\"check\">Decide, then check.</span></li>\n<li>An integration posts three related rows (an order and two order lines) that must all be created together or not at all. Over the Web API, how do you guarantee this? <span class=\"check\">Decide, then check.</span></li>\n<li>Your bulk loader starts getting HTTP 429 responses partway through. What is the correct code behavior, and what would be wrong? <span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>Use the <b>Dataverse Web API</b> (any-language HTTP client) with <b>OAuth 2.0 client-credentials (S2S)</b>: an Entra app registration authenticating by client secret or certificate. Create a matching <b>application user</b> in the environment and give it a least-privilege security role, otherwise calls are rejected. Acquire tokens with MSAL and send them as <code>Authorization: Bearer</code>.</li>\n<li>Use the optimized <b>UpsertMultiple</b> (or <code>ExecuteMultipleRequest</code> wrapping <code>UpsertRequest</code>s) with <b>ContinueOnError=true</b> so one bad row doesn't stop the batch, alternate keys for the upsert match, controlled parallelism, and 429 retry with backoff. Prefer the <code>*Multiple</code> messages over per-row Create for throughput.</li>\n<li>Put all three operations in a single <b>$batch changeset</b>. Everything in one changeset commits as one transaction, so either all rows are created or none are. (The SDK equivalent is <code>ExecuteTransactionRequest</code>.)</li>\n<li>Read the <b>Retry-After</b> header, wait that long, then retry with exponential backoff. Wrong: retrying immediately, or increasing concurrency to push through — both worsen the throttle. 429 is a signal to slow down, not to add load.</li>\n</ol></details>\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Which HTTP method updates an existing row through the Dataverse Web API, and which request header returns the updated row in the response?</li>\n<li>What is the difference between <code>ExecuteMultipleRequest</code> and <code>ExecuteTransactionRequest</code>?</li>\n<li>Name the three quantities the service protection limits measure over the 5-minute sliding window.</li>\n<li>For a daemon app with no user, what two things must be configured for it to call Dataverse successfully?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><b>PATCH</b> updates the row. Add <code>Prefer: return=representation</code> to get the updated record back in the response body (avoiding a second GET).</li>\n<li><code>ExecuteMultipleRequest</code> runs many requests for <b>throughput</b> and is <b>not</b> transactional (can ContinueOnError); <code>ExecuteTransactionRequest</code> runs them as a <b>single atomic transaction</b> — all commit or all roll back.</li>\n<li>Number of <b>requests</b> (6,000), combined <b>execution time</b> (20 minutes), and number of <b>concurrent requests</b> — all per user, per server. Exceeding any returns 429 with <code>Retry-After</code>.</li>\n<li>An <b>Entra app registration</b> (client credentials: secret or certificate) using the OAuth client-credentials flow, and a matching <b>application user with a security role</b> in the target environment.</li>\n</ol></details>" },
  { id: "5.4", domain: 5, title: "Process workloads by using Azure Functions and advanced cloud flows", ready: true,
    intro: "When logic must run outside the Dataverse plug-in sandbox — long-running jobs, scheduled work, or calls to third-party services — you offload it to Azure Functions or to advanced Power Automate cloud flows. This sub-skill covers Functions triggers and bindings, managed identities for credential-free auth, and Dataverse-connected flows with trigger filters, complex expressions, and Key Vault secrets.",
    html: "<h2>Notes</h2>\n<h3>Azure Functions: serverless compute for offloaded workloads</h3>\n<p>Azure Functions is an <b>event-driven, serverless</b> compute service. Use it when Power Platform logic would exceed the plug-in sandbox limits — the plug-in pipeline enforces a <b>2-minute execution timeout</b>, blocks most out-of-process calls, and is not meant for heavy or long-running work. A Function runs your code only when a <b>trigger</b> fires and you pay per execution (Consumption plan).</p>\n<ul>\n<li><b>When to choose a Function over a plug-in:</b> long-running processing, calling external/third-party REST APIs, aggregation or batch jobs, scheduled tasks, or anything needing libraries and compute the sandbox forbids.</li>\n<li><b>Hosting plans:</b> <b>Consumption</b> (pay-per-use, scales to zero, ~5-min default timeout, extendable), <b>Premium</b> (pre-warmed instances, VNet, no cold start, longer timeouts), and <b>Dedicated (App Service) plan</b> (predictable cost, runs on your App Service).</li>\n<li><b>Integration patterns:</b> a plug-in or flow calls the Function's HTTP endpoint; or the Function is fronted by a <b>custom connector</b> (sub-skill 5.2) so makers call it as an action; or Dataverse events are pushed to the Function via a <b>webhook / Service Bus endpoint</b> (domain 6).</li>\n</ul>\n<h3>Triggers, bindings, and long-running operations</h3>\n<p>Every Function has exactly one <b>trigger</b> (what starts it) and optional <b>input/output bindings</b> (declarative connections to data, so you avoid boilerplate SDK code). Bindings are configured in attributes (C#) or <code>function.json</code>.</p>\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Trigger</th><th>Fires when</th><th>Typical Power Platform use</th></tr></thead>\n<tbody>\n<tr><td><b>HTTP</b></td><td>An HTTP request hits the endpoint</td><td>Called synchronously by a plug-in, flow, or custom connector</td></tr>\n<tr><td><b>Timer</b></td><td>A CRON/NCRONTAB schedule elapses</td><td>Scheduled nightly sync, cleanup, or aggregation</td></tr>\n<tr><td><b>Queue / Service Bus</b></td><td>A message arrives on a queue/topic</td><td>Event-driven processing of Dataverse events (decoupled, reliable)</td></tr>\n<tr><td><b>Event Grid / Event Hub</b></td><td>An event is published</td><td>High-volume streaming / reactive integration</td></tr>\n</tbody>\n</table></div>\n<ul>\n<li><b>Scheduled vs event-driven:</b> a <b>Timer trigger</b> runs on a fixed schedule regardless of data; an <b>event-driven trigger</b> (queue, Service Bus, HTTP, Event Grid) runs in reaction to something happening. Prefer event-driven for reactive integration and Timer only for genuine time-based batch work.</li>\n<li><b>Long-running operations:</b> HTTP-triggered work that exceeds the timeout should use the <b>async request-reply</b> pattern (return 202 Accepted + a status URL) or move to <b>Durable Functions</b>, which orchestrate multi-step, stateful, long-running workflows (fan-out/fan-in, function chaining, human-interaction) with automatic checkpointing. For fire-and-forget from a flow, drop a message on a queue and let a queue-triggered Function do the work.</li>\n<li><b>Don't block a plug-in on a slow call:</b> a synchronous plug-in awaiting a slow Function will hit the 2-minute pipeline limit — use an <b>asynchronous</b> registration, a queue, or a webhook instead.</li>\n</ul>\n<h3>Managed identities for credential-free authentication</h3>\n<p>A <b>managed identity</b> gives an Azure resource (like your Function App) an automatically-managed identity in Microsoft Entra ID, so it can obtain OAuth tokens and call other services <b>without any secrets or connection strings in code or config</b>. Entra ID handles credential rotation.</p>\n<ul>\n<li><b>System-assigned:</b> tied to one resource, created and deleted with it (1:1 lifecycle). Good default for a single Function App.</li>\n<li><b>User-assigned:</b> a standalone Entra resource you create once and assign to <b>many</b> resources; survives independently. Use when several apps share one identity or you want the identity to outlive a resource.</li>\n<li><b>Usage:</b> grant the identity access (e.g. an <b>application user</b> + security role in Dataverse, or a role assignment / access policy on Key Vault, Storage, SQL), then acquire tokens via <code>DefaultAzureCredential</code> / <code>ManagedIdentityCredential</code>. No client secret to store or leak.</li>\n</ul>\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li>Plug-ins time out at <b>2 minutes</b> and can't do heavy/long work — offload to an Azure Function; register the plug-in as <b>asynchronous</b> or use a queue so you don't block the pipeline.</li>\n<li>One trigger per Function. <b>Timer = scheduled (CRON)</b>; <b>queue/Service Bus/HTTP/Event Grid = event-driven</b>. Durable Functions handle stateful long-running orchestration.</li>\n<li>Use a <b>managed identity</b> (system- or user-assigned) for credential-free service-to-service auth — no secrets in code. In Dataverse, the identity maps to an <b>application user</b> with a security role.</li>\n<li>In cloud flows, use <b>trigger filter columns + a trigger condition</b> to fire only on relevant changes; store secrets in <b>Azure Key Vault referenced via environment variables</b>, never as plain-text flow values.</li>\n</ul></div>\n<h3>Advanced cloud flows: the Dataverse connector and trigger filters</h3>\n<p>The <b>Microsoft Dataverse</b> connector is the primary way cloud flows react to and act on data. Its key trigger is <b>When a row is added, modified or deleted</b>.</p>\n<ul>\n<li><b>Change type &amp; scope:</b> pick Added / Modified / Deleted (or combinations) and a <b>scope</b> — Organization, Business Unit, Parent: Child Business Unit, or User — controlling which rows' changes the flow sees.</li>\n<li><b>Filter columns (Select columns):</b> list the columns that matter so the trigger fires <b>only when one of those columns changes</b>, reducing noise and needless runs.</li>\n<li><b>Trigger conditions / filter expression:</b> add an OData-style <b>Filter rows</b> expression or a <b>trigger condition</b> (an expression that must evaluate to <code>true</code>) so the flow starts only for qualifying rows — e.g. only high-value orders. This is far cheaper than running the flow then terminating.</li>\n<li><b>Run as:</b> choose whose security context the flow runs under — <b>Row owner</b>, <b>Modifying user</b>, or a specific user — which governs the privileges the flow's Dataverse steps use.</li>\n<li><b>Delay / concurrency:</b> tune to avoid loops (a flow that modifies the same table it triggers on) and to respect service limits.</li>\n</ul>\n<h3>Complex expressions and Azure Key Vault secrets</h3>\n<ul>\n<li><b>Complex expressions</b> use the Workflow Definition Language: functions like <code>coalesce()</code>, <code>if()</code>, <code>formatDateTime()</code>, <code>outputs()</code>, <code>triggerOutputs()</code>, <code>items()</code>, and string/collection helpers let you transform data inline instead of adding extra actions. Nest them to build conditions, e.g. a trigger condition like <code>@greater(triggerOutputs()?['body/totalamount'], 10000)</code>.</li>\n<li><b>Azure Key Vault secrets via environment variables:</b> create a <b>secret-type environment variable</b> that references a <b>Key Vault secret</b> (by vault + secret name). Flows and custom connectors read the secret at runtime through the environment variable — the value stays in Key Vault, moves cleanly across environments in a solution, and is never stored in the flow itself. The environment's identity must be granted <b>Get</b> access on the vault's secrets.</li>\n<li><b>Why it matters:</b> secrets in plain-text flow inputs are a security and ALM anti-pattern; environment variables + Key Vault keep credentials out of the solution and let each environment point at its own vault.</li>\n</ul>\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>A plug-in must call a partner REST API that sometimes takes 90+ seconds and can spike to several minutes. Users report the operation failing. How do you re-architect it? <span class=\"check\">Decide, then check.</span></li>\n<li>You need a job that recalculates account risk scores every night at 02:00 and also want a separate process to react the instant a high-value opportunity is created. Which trigger(s) do you choose? <span class=\"check\">Decide, then check.</span></li>\n<li>Your Function must write results back into Dataverse and read a secret from Key Vault, and security review forbids any stored credentials. How do you authenticate? <span class=\"check\">Decide, then check.</span></li>\n<li>A cloud flow on the Order table currently runs on every update and then terminates for 95% of rows. It must only run when the <i>status</i> column changes to <i>Approved</i> and the amount exceeds 10,000. How do you configure the trigger? <span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>Move the slow call out of the synchronous plug-in — it will hit the 2-minute pipeline limit. Register the plug-in <b>asynchronously</b> (or have it drop a message on a Service Bus / storage queue), and let a <b>queue-triggered Azure Function</b> call the partner API. For multi-step long work, use <b>Durable Functions</b> with the async request-reply pattern rather than blocking.</li>\n<li>Two triggers: a <b>Timer-triggered</b> Function with a CRON expression for the 02:00 nightly recalculation (scheduled), and an <b>event-driven</b> path for the instant reaction — a Dataverse <i>When a row is added</i> flow (or a queue/Service Bus-triggered Function) filtered to high-value opportunities. Don't poll on a timer for the reactive case.</li>\n<li>Enable a <b>managed identity</b> on the Function App (system-assigned for a single app, user-assigned if shared). Create a Dataverse <b>application user</b> for that identity with a least-privilege security role, and grant the identity <b>Get</b> on the Key Vault secret. Acquire tokens with <code>DefaultAzureCredential</code> — no secrets in code or config.</li>\n<li>On the <i>When a row is modified</i> trigger, set <b>Filter columns</b> to <code>statuscode</code> so it only fires when status changes, and add a <b>trigger condition</b> such as <code>@and(equals(triggerOutputs()?['body/statuscode'],'Approved'), greater(triggerOutputs()?['body/amount'],10000))</code>. The flow then never starts for the 95% — no wasted runs.</li>\n</ol></details>\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>What is the maximum execution time of a synchronous Dataverse plug-in, and what does that imply for long-running logic?</li>\n<li>Which Azure Functions trigger is time-based (scheduled), and which are event-driven? Give examples of each.</li>\n<li>Contrast a system-assigned and a user-assigned managed identity, and state what a managed identity lets you avoid.</li>\n<li>In a Dataverse cloud-flow trigger, what is the difference between <i>filter columns</i> and a <i>trigger condition</i>, and why use them?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><b>2 minutes.</b> Long-running or heavy work must be offloaded to an Azure Function (async plug-in, queue, or webhook), not run inline in the pipeline.</li>\n<li><b>Timer</b> trigger is scheduled (NCRONTAB expression). <b>Event-driven</b> triggers include HTTP, Queue/Service Bus, Event Grid, and Event Hub — they fire in reaction to a request or message rather than a clock.</li>\n<li><b>System-assigned</b> is bound 1:1 to a single resource and shares its lifecycle; <b>user-assigned</b> is a standalone Entra identity that can be shared across many resources and persists independently. Either lets you authenticate service-to-service <b>without storing secrets or connection strings</b> — Entra manages the credentials.</li>\n<li><b>Filter columns</b> restrict the trigger to fire only when specific columns change; a <b>trigger condition</b> is a boolean expression that must be true for the flow to start (e.g. amount &gt; 10,000). Together they stop the flow from running on irrelevant changes, saving runs and improving performance.</li>\n</ol></details>" },
  { id: "6.1", domain: 6, title: "Publish and consume Dataverse events", ready: true,
    intro: "Dataverse can push event data out of the platform to external listeners through service endpoints and webhooks. This guide covers how to register those endpoints with the Plug-in Registration Tool, the role of IServiceEndpointNotificationService in Azure-aware plug-ins, and how to choose between webhooks, Azure Service Bus, and Azure Event Hub.",
    html: "<h2>Notes</h2>\n<h3>The Azure integration model</h3>\n<ul>\n<li>Dataverse posts the plug-in <b>execution context</b> (a serialized <code>RemoteExecutionContext</code>) to an external listener when a registered event fires. This decouples heavy or external processing from the synchronous Dataverse transaction.</li>\n<li>Two delivery mechanisms exist: <b>service endpoints</b> that target Azure messaging (Service Bus and Event Hubs) via the Azure integration, and <b>webhooks</b> that target any HTTPS endpoint (for example an Azure Function or a custom web API).</li>\n<li>Both are registered as steps on the <b>event execution pipeline</b>, so they respect messages (Create/Update/Delete), filtering attributes, and pre/post images just like a plug-in step.</li>\n<li>Service Bus and Event Hub notifications are always <b>asynchronous</b> (queued by the Dataverse async service). Webhooks can be registered as <b>synchronous or asynchronous</b> steps.</li>\n</ul>\n<h3>IServiceEndpointNotificationService and Azure-aware plug-ins</h3>\n<ul>\n<li>An <b>Azure-aware plug-in</b> posts the execution context to the Azure Service Bus programmatically. It obtains <code>IServiceEndpointNotificationService</code> from the plug-in's <code>IServiceProvider</code> and calls its <code>Execute(EntityReference serviceEndpoint, IExecutionContext context)</code> method.</li>\n<li>The <code>EntityReference</code> passed to <code>Execute</code> points to the <b>serviceendpoint</b> record that was registered with the Plug-in Registration Tool; it identifies which Service Bus contract to post to.</li>\n<li>For a <b>two-way</b> or <b>REST</b> contract, <code>Execute</code> returns a string from the listener, which the plug-in can act on. For queue/one-way/topic contracts there is no return value.</li>\n<li>An Azure-aware plug-in is only required when you need <b>conditional</b> posting or a return value; for simple pass-through you register the service endpoint step directly and no custom plug-in code is needed.</li>\n</ul>\n<h3>Registering endpoints with the Plug-in Registration Tool</h3>\n<ul>\n<li>The <b>Plug-in Registration Tool</b> (PRT, installed via the <code>pac tool prt</code> command or NuGet) registers both service endpoints and webhooks and connects them to pipeline steps.</li>\n<li>For Service Bus / Event Hub: <b>Register New Service Endpoint</b>, paste the Azure <b>connection string</b> (SAS), then pick the contract/designation. Dataverse authenticates to Azure using the <b>SAS key</b> embedded in the endpoint configuration.</li>\n<li>For webhooks: <b>Register New Web Hook</b>, supply the HTTPS URL and choose an authentication scheme — <b>HttpHeader</b> or <b>WebhookKey</b> (query-string key). Azure Functions typically use the function key as a WebhookKey.</li>\n<li>After the endpoint exists, add a <b>step</b> (message + primary table + stage + sync/async) so events are actually posted.</li>\n</ul>\n<h3>Listening / consuming the events</h3>\n<ul>\n<li><b>Service Bus queue</b>: one listener reads and removes each message — competing-consumer, one message processed once. Good for guaranteed once-only line-of-business processing.</li>\n<li><b>Service Bus topic</b>: publish/subscribe — multiple subscriptions each get a copy, allowing several independent consumers.</li>\n<li><b>Event Hub</b>: high-throughput telemetry/streaming ingestion; consumers read via consumer groups and checkpoints (partitioned, replayable) rather than removing messages.</li>\n<li><b>Webhook</b>: the external service receives an HTTP POST with the JSON <code>RemoteExecutionContext</code> in the body; the handler must return HTTP 2xx. A synchronous webhook can throw to <b>cancel</b> the operation and surface an error to the user.</li>\n</ul>\n<div class=\"table-wrap\"><table>\n<thead><tr><th>Endpoint</th><th>Sync option</th><th>Consumption model</th><th>Best for</th></tr></thead>\n<tbody>\n<tr><td><b>Webhook</b></td><td>Sync or async</td><td>HTTP POST to one HTTPS handler; returns 2xx</td><td>Real-time call to Azure Functions / custom API; low volume; needs to block or return errors</td></tr>\n<tr><td><b>Service Bus queue</b></td><td>Async only</td><td>Single competing consumer removes message</td><td>Reliable once-only decoupled processing; buffering spikes</td></tr>\n<tr><td><b>Service Bus topic</b></td><td>Async only</td><td>Pub/sub, many subscribers each get a copy</td><td>Fan-out to multiple independent downstream systems</td></tr>\n<tr><td><b>Event Hub</b></td><td>Async only</td><td>Partitioned stream, consumer groups, replayable</td><td>High-volume telemetry / analytics ingestion</td></tr>\n</tbody>\n</table></div>\n<div class=\"callout callout-key\"><b>Exam-critical</b><ul>\n<li><b>IServiceEndpointNotificationService.Execute</b> takes an <code>EntityReference</code> to the registered <b>serviceendpoint</b> record and posts the context to <b>Azure Service Bus</b> from an Azure-aware plug-in.</li>\n<li>Service Bus and Event Hub steps are <b>asynchronous only</b>; only <b>webhooks</b> can be registered <b>synchronously</b> (and can therefore cancel the operation).</li>\n<li>Use a <b>webhook</b> for a single real-time HTTPS listener (Azure Function), a <b>queue</b> for once-only processing, a <b>topic</b> for pub/sub fan-out, and an <b>Event Hub</b> for high-volume streaming.</li>\n<li>Webhook authentication is <b>HttpHeader</b> or <b>WebhookKey</b>; Service Bus endpoints authenticate with a <b>SAS connection string</b>.</li>\n</ul></div>\n<h2>Scenario decisions</h2>\n<ol class=\"steps\">\n<li>An order row is updated and you must call an existing Azure Function over HTTPS in real time, blocking the save if the function reports the order is invalid. Which endpoint type and registration mode do you choose? <span class=\"check\">Decide, then check.</span></li>\n<li>Five separate downstream systems (billing, shipping, analytics, CRM sync, audit) must each independently receive every account creation, with no system consuming another's copy. Which Azure messaging option? <span class=\"check\">Decide, then check.</span></li>\n<li>Your plug-in should post the execution context to Service Bus only when an opportunity's estimated value exceeds a threshold, and read a confirmation string back from the listener. What do you implement and which service do you call? <span class=\"check\">Decide, then check.</span></li>\n<li>Millions of IoT-style status updates per day must be streamed into an analytics pipeline that can replay them; guaranteed single-consumer removal is not required. Which endpoint? <span class=\"check\">Decide, then check.</span></li>\n</ol>\n<details class=\"answers\"><summary>Model answers</summary><ol>\n<li>Register a <b>webhook</b> (Register New Web Hook in the PRT) pointing at the Function URL, secured with a <b>WebhookKey</b>, and register the step as <b>synchronous</b> on the Update message so a thrown exception cancels the save. Service Bus/Event Hub cannot run synchronously.</li>\n<li>An <b>Azure Service Bus topic</b> with five subscriptions — publish/subscribe delivers a copy to every subscriber. A queue would let only one consumer take each message.</li>\n<li>Write an <b>Azure-aware plug-in</b>: get <code>IServiceEndpointNotificationService</code> from the service provider, apply your value condition in code, and call <code>Execute(serviceEndpointRef, context)</code> against a <b>two-way (or REST) contract</b> to receive the returned string.</li>\n<li>An <b>Azure Event Hub</b> — built for high-throughput, partitioned, replayable event streams consumed via consumer groups, unlike a queue's once-only removal.</li>\n</ol></details>\n<h2>Self-check</h2>\n<ol class=\"quiz\">\n<li>Which service does an Azure-aware plug-in obtain from <code>IServiceProvider</code> to post the execution context to Azure Service Bus, and what argument identifies the target endpoint?</li>\n<li>True or false: an Azure Service Bus service endpoint step can be registered as a synchronous step so it can cancel the Dataverse operation.</li>\n<li>What is delivered to a webhook handler in the HTTP request body, and what response is expected?</li>\n<li>In the Plug-in Registration Tool, which two authentication options are offered when registering a webhook?</li>\n</ol>\n<details class=\"answers\"><summary>Answers</summary><ol>\n<li><code>IServiceEndpointNotificationService</code>; its <code>Execute</code> method takes an <code>EntityReference</code> to the registered <b>serviceendpoint</b> record identifying which Service Bus contract to post to.</li>\n<li><b>False.</b> Service Bus (and Event Hub) endpoints are <b>asynchronous only</b>. Only a <b>webhook</b> can be registered synchronously and thereby cancel the operation.</li>\n<li>The serialized <b>RemoteExecutionContext</b> as <b>JSON</b>; the handler must return an HTTP <b>2xx (success)</b> status. A synchronous webhook throwing an error cancels the operation.</li>\n<li><b>HttpHeader</b> (authentication values sent as HTTP headers) and <b>WebhookKey</b> (a key passed on the query string) — Azure Functions commonly use the function key as a WebhookKey.</li>\n</ol></details>" },
  { id: "6.2", domain: 6, title: "Perform data synchronization with Dataverse", ready: true,
    intro: "This sub-skill is about keeping an external system and Dataverse in agreement efficiently: pulling only what changed instead of re-reading everything, matching rows by a business key instead of a GUID, and writing back with a single message that creates or updates as needed. The exam wants you to pick change tracking, alternate keys, and Upsert for the right leg of an integration.",
    html: "<h2>Notes</h2><h3>Change tracking (outbound delta sync)</h3><ul><li><b>Change tracking</b> lets an external system read only the rows that were <b>created, updated, or deleted</b> since its last read, instead of doing a full re-sync of the whole table.</li><li>It must be <b>enabled per table</b> — set <b>Track changes</b> on the table definition (also exposed as the <b>ChangeTrackingEnabled</b> property). Only then does Dataverse maintain the version data needed for deltas.</li><li>With the <b>SDK for .NET</b>, call <b>RetrieveEntityChangesRequest</b>. The response returns an <b>EntityChangeList</b> plus a <b>DataToken</b> (delta token). You persist the token and pass it back on the next request as the <b>DataVersionToken</b> so the service returns only changes since that token.</li><li>Results come back as change items typed <b>NewOrUpdatedItem</b> (the changed <b>Entity</b>) and <b>RemovedOrDeletedItem</b> (an <b>EntityReference</b> for a row that was deleted or fell out of scope) — so your handler can apply upserts and deletes on the external side.</li><li>With the <b>Web API</b>, request the preference header <b>Prefer: odata.track-changes</b> on a query. The response includes an <b>@odata.deltaLink</b>; calling that delta link later returns only the changes, with deleted rows expressed as <b>@removed</b> entries.</li><li>A stored token can <b>expire</b> (for example after the table is reconfigured or the retention window passes); handle the expired-token error by falling back to a full read and capturing a fresh token.</li></ul><h3>Alternate keys (matching rows by a business key)</h3><ul><li>An <b>alternate key</b> lets you identify a Dataverse row by one or more <b>business columns</b> with unique values (an order number, an ERP customer code) instead of the system <b>GUID</b> — essential when the external system does not store Dataverse primary keys.</li><li>A key can be a <b>single column or a combination</b> of columns, and a table can have up to <b>5</b> alternate keys. Supported column types include <b>Decimal, Integer, String, DateTime, Lookup, and Choice</b>; unsupported types cannot be part of a key.</li><li>Creating a key runs an <b>asynchronous system job</b> that builds the supporting index; the key must reach status <b>Active</b> before it can be used. If duplicate values already exist, the job fails and the key stays inactive.</li><li>Web API addresses a row by alternate key with key/value syntax: <b>accounts(accountnumber='ABC123')</b>; a compound key lists each column, e.g. <b>(keycol1='a',keycol2=2)</b>.</li><li>Alternate keys are what make <b>Upsert</b>, plus Retrieve and Update-by-key, work from an integration that only knows the business identifier.</li></ul><h3>UpsertRequest (idempotent write-back)</h3><ul><li><b>Upsert</b> = update-or-insert: in a single message Dataverse <b>creates</b> the row if it does not exist and <b>updates</b> it if it does. This makes an inbound feed <b>idempotent</b> and avoids duplicate records.</li><li>In the SDK, send an <b>UpsertRequest</b> whose <b>Target</b> is an Entity keyed by its <b>alternate key</b> (or primary id). The <b>UpsertResponse</b> returns <b>RecordCreated</b> (true if it inserted) and the <b>Target</b> <b>EntityReference</b> of the affected row.</li><li>Existence is decided by the <b>key</b> on the Target: if a row matches the primary id or alternate key it is updated; otherwise a new row is created (and if you supplied the id/key value, that value is used).</li><li>With the Web API, Upsert is a <b>PATCH</b> to a row addressed by alternate key. Control the behavior with headers: <b>If-Match: *</b> forces <b>update-only</b> (fails if absent), and <b>If-None-Match: *</b> forces <b>create-only</b> (fails if present).</li><li>Upsert saves the round trip of first querying to see whether the row exists — the pattern of choice for repeatedly importing the same external dataset.</li></ul><div class=\"table-wrap\"><table><thead><tr><th>Need</th><th>Mechanism</th><th>Key detail</th></tr></thead><tbody><tr><td>Pull only what changed to an external store</td><td><b>Change tracking</b></td><td>Enable per table; persist the delta token / <b>@odata.deltaLink</b></td></tr><tr><td>Detect rows deleted since last read</td><td>Change tracking</td><td><b>RemovedOrDeletedItem</b> / <b>@removed</b> entries</td></tr><tr><td>Identify a row without its GUID</td><td><b>Alternate key</b></td><td>Up to 5 per table; must be <b>Active</b>; unique business columns</td></tr><tr><td>Import a feed without creating duplicates</td><td><b>UpsertRequest</b></td><td>Create-or-update by key; <b>RecordCreated</b> flag in response</td></tr></tbody></table></div><div class=\"callout callout-key\"><b>Exam-critical</b><ul><li><b>Change tracking must be enabled on the table first</b>; then persist the returned token (SDK <b>DataToken</b>) or <b>@odata.deltaLink</b> (Web API <b>Prefer: odata.track-changes</b>) and pass it back to get only deltas.</li><li>Deletes surface as <b>RemovedOrDeletedItem</b> (SDK) or <b>@removed</b> (Web API) — change tracking reports deletions, not just creates and updates.</li><li><b>Alternate keys</b> let integrations address rows by business columns instead of GUID; max <b>5</b> per table and the key must be <b>Active</b> before use.</li><li><b>UpsertRequest</b> creates or updates in one call; check <b>RecordCreated</b> on the response. In the Web API, <b>If-Match: *</b> = update-only, <b>If-None-Match: *</b> = create-only.</li><li>Combine them: <b>change tracking</b> for the outbound leg, <b>alternate keys + Upsert</b> for the idempotent inbound leg.</li></ul></div><h2>Scenario decisions</h2><ol class=\"steps\"><li>A nightly job copies Dataverse contacts into a data warehouse. The full table has millions of rows and the current job re-reads all of them every night, which is too slow. <span class=\"check\">Decide, then check.</span></li><li>An ERP sends customer records identified only by an ERP customer code; the same customers arrive repeatedly and must not create duplicates in Dataverse, and the integration never stores Dataverse GUIDs. <span class=\"check\">Decide, then check.</span></li><li>The warehouse sync must also remove contacts from the warehouse when they are deleted in Dataverse, not just add and change them. <span class=\"check\">Decide, then check.</span></li><li>You try to make the ERP customer code an alternate key, but the key stays inactive and the integration cannot use it. <span class=\"check\">Decide, then check.</span></li></ol><details class=\"answers\"><summary>Model answers</summary><ol><li>Enable <b>Track changes</b> on the Contact table and switch the job to <b>change tracking</b>: first run captures a delta token (SDK <b>RetrieveEntityChangesRequest</b> / Web API <b>Prefer: odata.track-changes</b>), and each subsequent run passes the stored token / <b>@odata.deltaLink</b> to read only rows changed since.</li><li>Define an <b>alternate key</b> on the ERP customer-code column, then write each incoming record with an <b>UpsertRequest</b> (Web API PATCH by that key). Upsert updates the existing row or inserts a new one, so re-sends are idempotent and produce no duplicates.</li><li>Change tracking already reports deletions: process <b>RemovedOrDeletedItem</b> (SDK) or <b>@removed</b> (Web API) entries from the delta and delete the matching rows in the warehouse.</li><li>The alternate-key <b>system job failed</b> — usually because <b>duplicate values</b> already exist in that column, or the column type is unsupported. Resolve the duplicates (or pick valid unique columns), then recreate the key and wait for it to reach <b>Active</b>.</li></ol></details><h2>Self-check</h2><ol class=\"quiz\"><li>What must be true of a table before an external system can use change tracking against it, and what does the client persist between runs?</li><li>How does change tracking communicate that a row was deleted?</li><li>How many alternate keys can a table have, and what status must a key reach before it can be used?</li><li>What does UpsertRequest do when the target row does not yet exist, and how do you tell which happened?</li></ol><details class=\"answers\"><summary>Answers</summary><ol><li><b>Track changes</b> must be <b>enabled</b> on the table (ChangeTrackingEnabled). The client persists the <b>delta token</b> (SDK <b>DataToken</b>) or <b>@odata.deltaLink</b> and passes it back to receive only changes since the last read.</li><li>Via <b>RemovedOrDeletedItem</b> change items in the SDK response, or <b>@removed</b> entries when following the Web API delta link.</li><li>Up to <b>5</b> alternate keys per table; the key's supporting index job must complete so the key is <b>Active</b> (a create-only key with existing duplicates will fail).</li><li>It <b>creates</b> the row (insert); if it exists it <b>updates</b> it. Check <b>RecordCreated</b> on the UpsertResponse (or infer from create-only/update-only headers in the Web API) to know whether it inserted or updated.</li></ol></details>" },
];

const MASTERY_REFS = {
 "1.1": {
  "module": {
   "title": "Apply business logic in Microsoft Dataverse",
   "url": "https://learn.microsoft.com/en-us/power-apps/maker/data-platform/processes"
  },
  "units": [
   {
    "title": "Types of tables (standard, activity, virtual, elastic)",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/data-platform/types-of-entities"
   },
   {
    "title": "Elastic tables for developers",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/elastic-tables"
   },
   {
    "title": "Security roles and privileges",
    "url": "https://learn.microsoft.com/en-us/power-platform/admin/security-roles-privileges"
   },
   {
    "title": "Data policies (DLP) for Power Platform",
    "url": "https://learn.microsoft.com/en-us/power-platform/admin/wp-data-loss-prevention"
   }
  ]
 },
 "1.2": {
  "module": {
   "title": "Canvas component overview — reusable low-code components with custom properties",
   "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-component"
  },
  "units": [
   {
    "title": "Power Apps component framework (PCF) overview — code components for model-driven and canvas apps",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/component-framework/overview"
   },
   {
    "title": "Custom connectors overview — wrap a REST API, lifecycle, share and certify",
    "url": "https://learn.microsoft.com/en-us/connectors/custom-connectors/"
   },
   {
    "title": "Create and use custom APIs — code-first custom messages in Dataverse",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/custom-api"
   },
   {
    "title": "Create a cloud flow in Power Automate — triggers and actions",
    "url": "https://learn.microsoft.com/en-us/power-automate/get-started-logic-flow"
   }
  ]
 },
 "2.1": {
  "module": {
   "title": "Create and manage environments in the Power Platform admin center",
   "url": "https://learn.microsoft.com/en-us/power-platform/admin/create-environment"
  },
  "units": [
   {
    "title": "Security roles and privileges in Microsoft Dataverse",
    "url": "https://learn.microsoft.com/en-us/power-platform/admin/security-roles-privileges"
   },
   {
    "title": "Use single-tenant server-to-server authentication (application users)",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/use-single-tenant-server-server-authentication"
   }
  ]
 },
 "2.2": {
  "module": {
   "title": "Application lifecycle management (ALM) overview",
   "url": "https://learn.microsoft.com/en-us/power-platform/alm/overview-alm"
  },
  "units": [
   {
    "title": "Use environment variables in solutions",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/data-platform/environmentvariables"
   },
   {
    "title": "Solution layers and merge behavior",
    "url": "https://learn.microsoft.com/en-us/power-platform/alm/solution-layers-alm"
   },
   {
    "title": "Overview of pipelines in Power Platform",
    "url": "https://learn.microsoft.com/en-us/power-platform/alm/pipelines"
   },
   {
    "title": "Power Platform Build Tools for Azure DevOps",
    "url": "https://learn.microsoft.com/en-us/power-platform/alm/devops-build-tools"
   }
  ]
 },
 "3.1": {
  "module": {
   "title": "Microsoft Power Fx overview",
   "url": "https://learn.microsoft.com/en-us/power-platform/power-fx/overview"
  },
  "units": [
   {
    "title": "Formula reference for canvas apps",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/formula-reference"
   },
   {
    "title": "Component library",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/component-library"
   },
   {
    "title": "Start a flow in a canvas app",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/using-logic-flows"
   }
  ]
 },
 "3.2": {
  "module": {
   "title": "How to create performant Power Apps",
   "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-performant-apps-overview"
  },
  "units": [
   {
    "title": "Live monitor overview",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/monitor-overview"
   },
   {
    "title": "Understand delegation in a canvas app",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/delegation-overview"
   }
  ]
 },
 "4.1": {
  "module": {
   "title": "Apply business logic using client scripting in model-driven apps using JavaScript",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/model-driven-apps/client-scripting"
  },
  "units": [
   {
    "title": "Client API Reference for model-driven apps",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/model-driven-apps/clientapi/reference"
   },
   {
    "title": "Events in forms and grids",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/model-driven-apps/clientapi/events-forms-grids"
   },
   {
    "title": "Xrm.WebApi (Client API reference)",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/model-driven-apps/clientapi/reference/xrm-webapi"
   }
  ]
 },
 "4.2": {
  "module": {
   "title": "Power Apps component framework overview",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/component-framework/overview"
  },
  "units": [
   {
    "title": "Create and build a code component",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/component-framework/create-custom-controls-using-pcf"
   },
   {
    "title": "Manifest schema reference",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/component-framework/manifest-schema-reference/"
   },
   {
    "title": "Create your first component (init, updateView, getOutputs, destroy)",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/component-framework/implementing-controls-using-typescript"
   }
  ]
 },
 "5.1": {
  "module": {
   "title": "Use plug-ins to extend business processes",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/plug-ins"
  },
  "units": [
   {
    "title": "Event framework",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/event-framework"
   },
   {
    "title": "Register a plug-in",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/register-plug-in"
   },
   {
    "title": "Understand the execution context",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/understand-the-data-context"
   },
   {
    "title": "Create and use custom APIs",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/custom-api"
   }
  ]
 },
 "5.2": {
  "module": {
   "title": "Custom connectors overview — build, secure, describe, use, share, certify",
   "url": "https://learn.microsoft.com/en-us/connectors/custom-connectors/"
  },
  "units": [
   {
    "title": "Create a custom connector from an OpenAPI definition — import & review, auth, definition",
    "url": "https://learn.microsoft.com/en-us/connectors/custom-connectors/define-openapi-definition"
   },
   {
    "title": "Policy templates — modify connector behavior at runtime, environment variables",
    "url": "https://learn.microsoft.com/en-us/connectors/custom-connectors/policy-templates"
   },
   {
    "title": "Extend an OpenAPI definition — x-ms-* extensions, triggers, dynamic values/schema",
    "url": "https://learn.microsoft.com/en-us/connectors/custom-connectors/openapi-extensions"
   }
  ]
 },
 "5.3": {
  "module": {
   "title": "Use the Dataverse Web API",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/webapi/overview"
  },
  "units": [
   {
    "title": "Use the Organization service (SDK for .NET)",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/org-service/overview"
   },
   {
    "title": "Use OAuth authentication with Dataverse",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/authenticate-oauth"
   },
   {
    "title": "Service protection API limits",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/api-limits"
   }
  ]
 },
 "5.4": {
  "module": {
   "title": "Azure Functions overview",
   "url": "https://learn.microsoft.com/en-us/azure/azure-functions/functions-overview"
  },
  "units": [
   {
    "title": "Triggers and bindings in Azure Functions",
    "url": "https://learn.microsoft.com/en-us/azure/azure-functions/functions-triggers-bindings"
   },
   {
    "title": "Managed identities for Azure resources overview",
    "url": "https://learn.microsoft.com/en-us/entra/identity/managed-identities-azure-resources/overview"
   },
   {
    "title": "Integrate Power Automate flows with Dataverse",
    "url": "https://learn.microsoft.com/en-us/power-automate/dataverse/overview"
   },
   {
    "title": "When a row is added, modified or deleted trigger",
    "url": "https://learn.microsoft.com/en-us/power-automate/dataverse/create-update-delete-trigger"
   },
   {
    "title": "Use environment variables for Azure Key Vault secrets",
    "url": "https://learn.microsoft.com/en-us/power-apps/maker/data-platform/environmentvariables-azure-key-vault-secrets"
   }
  ]
 },
 "6.1": {
  "module": {
   "title": "Azure Service Bus integration for Dataverse",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/azure-integration"
  },
  "units": [
   {
    "title": "Use webhooks to create external handlers for server events",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/use-webhooks"
   },
   {
    "title": "Work with event data in an Azure Event Hub solution",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/work-event-data-azure-event-hub-solution"
   },
   {
    "title": "Write a custom Azure-aware plug-in",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/write-custom-azure-aware-plugin"
   }
  ]
 },
 "6.2": {
  "module": {
   "title": "Use change tracking to synchronize data with external systems",
   "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/use-change-tracking-synchronize-data-external-systems"
  },
  "units": [
   {
    "title": "Work with alternate keys",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/define-alternate-keys-entity"
   },
   {
    "title": "Use Upsert to insert or update a record",
    "url": "https://learn.microsoft.com/en-us/power-apps/developer/data-platform/use-upsert-insert-update-record"
   }
  ]
 }
};
